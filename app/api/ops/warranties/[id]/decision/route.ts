import { OpsDomainError } from "@/lib/ops/errors";
import { decideWarrantyCoverage } from "@/lib/ops/warranty-commands";
import { formText,getOpsRequestContext,opsApiError } from "@/lib/server/ops-request-context";
import { relativeRedirect303 } from "@/lib/server/relative-redirect";
const decisions=new Set(["covered","not_covered","split"]);const charges=new Set(["customer_responsible","warranty_covered","split"]);
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const context=await getOpsRequestContext(["executive","facilities","regional"]);const{id}=await params;const form=await request.formData();const coverageDecision=formText(form,"coverageDecision",{required:true});const customerChargeStatus=formText(form,"customerChargeStatus",{required:true});if(!decisions.has(coverageDecision)||!charges.has(customerChargeStatus))throw new OpsDomainError("VALIDATION","Coverage decision is invalid");await decideWarrantyCoverage({organizationId:context.session.organizationId,warrantyCaseId:id,actor:context.actor,coverageDecision:coverageDecision as "covered"|"not_covered"|"split",customerChargeStatus:customerChargeStatus as "customer_responsible"|"warranty_covered"|"split",invoiceHold:formText(form,"invoiceHold",{required:true})==="retain",diagnosis:formText(form,"diagnosis",{required:true,max:2000}),reason:formText(form,"reason",{required:true,max:2000})},{repository:context.repository});return relativeRedirect303(`/app/warranties/${encodeURIComponent(id)}?updated=coverage`)}catch(error){return opsApiError(error)}}
