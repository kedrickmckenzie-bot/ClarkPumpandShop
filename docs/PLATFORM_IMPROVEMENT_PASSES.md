# Platform improvement passes

This is the persistent execution checklist for the September 14, 2026 review. Read it at the start of platform work and update it before ending a session. The approved rebuild blueprint remains the product authority; [the dated review](PLATFORM_REVIEW_2026-09-14.md) records the original evidence. Older completion notes are not proof of current behavior.

## Checkpoint

- **Current priority (user clarification, September 15): desktop demo readiness first. Mobile acceptance is limited to vendor check-in/check-out.** Finish the desktop demo journey before further scalability-only migrations or broad mobile polish. Keep every unfinished item below recorded; this sequencing change does not mark those gates complete.

- Latest publication: private Sites version **53**, September 15 at 22:09:21 UTC, source `18f1015`, with small personal planning scenarios, created-date history filters and scoped vendor drill-through. Seed/typecheck/lint, 54 workflow checks, resolved full-suite failures and both builds are recorded below. User confirmed earlier Render no-login access works; the current source is pushed to GitHub, but this batch's live Render deployment has not been verified.
- Scheduled continuation is **paused at the user's request**. Do not restart the heartbeat. Current work prioritizes existing-screen consolidation and c-store usability, not duplicate dashboards.
- Completed passes: **1 — role/scope and accountability correctness**, **2 — trustworthy metrics and drill-through**, and **3 — manager and store workflow usability**. Pass 4 has three verified items; **P4-02 is reopened** for the owner-brief regional-scope counterexample found during Pass 5.
- Current product pass: **5 — bounded reads and maintainable persistence**, started under the user's authorization to continue all remaining passes automatically while away. **26 of 40 items complete; 14 remain**. P5-01/P5-04/P5-05 are complete; P5-02/P5-03/P5-06 remain open. Finish the reopened P4-02 while auditing linked record/program readers. No later pass is complete.
- Completed exception: **P6-08 — populated D1 upgrade repair**, pulled forward after the first private publication failed; the repaired publication succeeded. All original item IDs remain unchanged.
- Completion means acceptance evidence is recorded below, not merely that code was edited.
- Deployment and real-customer readiness are separate gates. Fictional preview controls are not production authentication.

## How to execute and remember

**Standing acceptance rule for every pass:** user experience and interface polish come first. Use short, plain words, obvious next actions, sensible defaults, and clean desktop/phone layouts. Remove long explanatory boxes and repeated instructions. For the current demo, validate operator journeys on desktop; mobile validation is required for vendor check-in/check-out only. Validate the affected journey visually as well as testing its behavior. A correct calculation behind confusing UI is unfinished work.

Keep the IDs below stable. Mark items `[x]` only after their acceptance checks pass; leave partial items unchecked with an explicit note. Append discoveries instead of silently expanding or dropping an item. After each pass record changes, checks, unresolved failures and the next starting point. On resume, inspect the working tree and this checkpoint before changing code. Never infer that an unchecked later pass was completed by an earlier one.

The order groups shared data models, UI surfaces and validation to avoid repeatedly changing the same code. Passes 1–3 stabilize behavior before architectural extraction. Pass 4 establishes production boundaries before pass 5 changes query paths. Pass 6 covers infrastructure-dependent proof. Pass 7 is the final cross-platform acceptance gate. Update documentation during every pass, not just at the end.

## Pass 1 — role/scope and accountability correctness

- [x] P1-01 Fix preview role **and edition** cookies for page/API parity; migrate old path-scoped cookies. Prove an ordinary cookie round trip keeps Store 104 scope on exports and rejects facilities-only actions.
- [x] P1-02 Retire the applicable held-work provider obligation when a vendor starts service. Diagnosis checkout must identify the manager and findings-review action before and after the hold deadline; preserve unrelated legitimate tasks. Cover fixture and persisted SQL adapters.
- [x] P1-03 Audit the other held-work entry points (adding work to an active visit, confirmed-visit pickup and service-run paths) for the same obsolete-task projection. Cover applicable paths without duplicating or erasing source evidence.
- [x] P1-04 Verify page identity, mutation actor attribution, permissions and audit actor remain consistent; retain the explicit fictional-preview boundary.

## Pass 2 — trustworthy metrics and drill-through

- [x] P2-01 Make pending-request count and destination share submitted/under-review IDs, including search, scope and pagination. The five pending requests must not link to all 100 reports.
- [x] P2-02 Separate work ready to issue from work awaiting a vendor response; share cohort definitions between counts, labels and queue filters.
- [x] P2-03 Reconcile equipment spotlight spending with its destination. Preserve cost basis, planned/reactive cohort and reporting window; include upper as-of bounds. Regress the Store 115 $0/$440 example.
- [x] P2-04 Inventory every principal dashboard metric, segment, exception and outlier. Test exact supporting IDs/counts and preservation of scope, period, taxonomy, cost basis, cohort and classification coverage; make omitted segments explicit.
- [x] P2-05 Verify onsite labels distinguish active visits from distinct vendors. Verify invoice counts distinguish invoice records, references and allocated/unmatched amounts without double counting.
- [x] P2-06 Verify PM numerator/denominator/window/source occurrences and lifecycle rules are transparent; no inferred downtime or automatic replacement/payment decisions.

## Pass 3 — manager and store workflow usability

- [x] P3-01 Make store and problem sufficient for basic work creation with valid default priority and choose-later routing in UI and API. Preserve deferred category/asset/component classification.
- [x] P3-02 Align internal-assignment instructions with the actual command requirements. Test internal, outside and blended fulfillment, vendor search and issue/reissue references.
- [x] P3-03 Stack phone section headings, descriptions and actions; visually verify actual 375/390 px layouts, readable text and touch controls on store and public visit views.
- [x] P3-04 Consolidate the work-order case into one dominant accountable state/owner/action/due/escalation header and concise evidence summary; preserve detailed source facts in appropriate tabs.
- [x] P3-05 Restore useful attention rows on the facilities home and prioritize immediate responsibilities, critical work, PM exceptions and store outliers. Validate manager roles and the six-destination navigation contract.
- [x] P3-06 Audit loading, empty, error, partial and permission states, labels, visible scope, keyboard/form behavior and every primary action. No dead controls or misleading optional/required instructions.
- [x] P3-07 Complete store creation, no-WO visit reconciliation and cross-channel visit journeys. Verify a reconciliation failure has atomic behavior or explicit recoverable state without losing/duplicating either record.

## Pass 4 — production identity and domain safeguards

- [x] P4-01 Resolve authenticated users to active organization memberships; isolate preview personas and fail closed outside the fictional preview. Verify missing identity, revoked membership and multiple organizations.
- [ ] P4-02 Verify organization-first access on every read, command, search, aggregate, export, private file, public token and job; add cross-tenant and cross-store adversarial tests. **Reopened, repair in progress:** owner-summary/source and record-check readers now pass organization and location scope. Actual regional/scoped-facilities loader and complete owner-brief page tests reject snapshot access. Audit linked PM/record readers before restoring this broad gate; earlier passing boundary tests did not cover the original reader.
- [x] P4-03 Verify all channels use shared commands with transactional audit, idempotent retries and optimistic concurrency. Test append-only amendments for reports, issuance, responses, visits, costs, allocations and deadlines.
- [x] P4-04 Verify purpose-bound, expiring, hashed action tokens and vendor-eligible work selection; no-WO service remains possible and location remains check-in/out only with explicit evidence status.

## Pass 5 — bounded reads and maintainable persistence

- [x] P5-01 Inventory remaining snapshot consumers; replace dashboard snapshots with scoped repository aggregates and source queries with stable pagination and indexed ordering. Main overview, owner brief, record checks and full review queue now use native scoped queries; their full totals/source pages and authenticated no-snapshot tests are recorded below. Remaining record/program/action-detail, creation, governance, API and Trends consumers stay explicit in the query inventory for P5-02/P5-03.
- [ ] P5-02 Replace record/program/lifecycle full snapshots with bounded feature queries. Prove scoped callers do not fetch the whole tenant and drill-through remains exact.
- [ ] P5-03 Extract feature presenters and command modules along domain boundaries while preserving shared commands and all evidence semantics; avoid introducing parallel application stacks.
- [x] P5-04 Replace PostgreSQL dependence on D1 SQL translation with a provider-neutral repository boundary and native queries; retain D1/R2 adapters and migration parity during the active hosted preview.
- [x] P5-05 Audit imports/routes before retiring legacy cstore/demo/domain/root stacks. Delete only proven unused code and verify routes/builds afterward.
- [ ] P5-06 Verify production import, stable store IDs/numbers/addresses/aliases, optional operating hierarchy and independent taxonomy. Keep the presentation at exactly 15 stores/five vendors; validate one-store and 65-store fixtures separately.

## Pass 6 — delivery, operations and deployment proof

- [ ] P6-01 Make issuance delivery durable and retryable across crashes between issuance, provider delivery and audit. Preserve immutable revisions and secure token handling; prove no duplicate authorization or misleading delivered status.
- [ ] P6-02 Verify PM recurrence, SLA/outbox/compliance workers are idempotent, observable and recoverable. Exercise provider failures and retry exhaustion; external delivery requires configured infrastructure and authorized recipients.
- [ ] P6-03 Verify private evidence uploads/downloads, tenant/file permissions, retention and S3-compatible storage portability; do not use ephemeral production disk.
- [ ] P6-04 Validate real PostgreSQL migrations, DATABASE_URL, injected PORT, health checks and worker entry points. Create no Render project/manifest until intentionally authorized.
- [ ] P6-05 Run backup/restore, rollback and alert-routing drills with recorded outcomes. Prove agreed recovery targets (review references RPO ≤1 hour/RTO ≤4 hours) on actual infrastructure.
- [ ] P6-06 Measure real-backend multi-user 65-store query latency, concurrency and failure recovery; fixture size and local development timings are not throughput proof.
- [x] P6-07 Prepare an exact validated hosted version and verify its access policy before publication. Record deployment status separately from local validation and production readiness.
- [x] P6-08 Repair the populated D1 migration failure discovered during Pass 1 publication. Preserve visit-work rows and their verification records under enforced foreign keys, verify an upgrade with existing data, and confirm the private publication succeeds without resetting the tenant.

## Pass 7 — acceptance, documentation and release decision

- [ ] P7-01 Add repeatable real-browser coverage for role-cookie round trips, native form validation, metric destinations and mobile layout. Clearly distinguish this from the existing Vitest `test:e2e` suite.
- [x] P7-02 Run all required commands sequentially enough to avoid resource-contention false failures: db:seed, typecheck, lint, npm test, test:e2e and build. Record failures accurately; also run build:render when portability changes.
- [ ] P7-03 Complete the required browser matrix: manager search/drill-down; store creation; deferred/internal/outside work routing; vendor action link; cross-channel visit; no-WO exception; invoice safeguards; PM/lifecycle; responsive store/mobile. Record which journeys actually mutated data and which were inspection only.
- [ ] P7-04 Reconcile README/demo/status docs with verified implementation and deterministic seed counts. Keep historical reviews dated, centralize current status and verify all temporary branding uses configuration.
- [ ] P7-05 Review every remaining unchecked item and external dependency before declaring release readiness. No principal metric or primary workflow may end at a dead control; no production claim without production evidence.

Additional discovery for P7-03: PM and lifecycle program layouts currently nest a second `main` inside the operator shell's `main`. Consolidate those landmarks during final accessibility acceptance; Pass 4 inspected these views without claiming that structural issue fixed.

## Evidence log

### Baseline — September 14, 2026

Review at commit `6330d5e`: seed/typecheck/lint/e2e/build/Render build passed; 144 sampled routes passed. Full test run: 876 passed/3 failed (one reproducible held-work assertion, two timeouts under overlapping workload). Isolated rerun retained the held-work failure. Browser findings and inspection limitations are recorded in the linked review. Existing untracked `dev4.log` is unrelated and must be preserved.

### Pass 1 — complete, September 14, 2026

Implemented shared root-path preview cookies with explicit expiration of the older `/app` cookie on role/package selection. Existing open preview sessions should apply their role/package once after the update to migrate the older cookie. This fixes preview consistency; production identity remains P4-01.

Held-work check-in and active-visit pickup now replace only a relevant service-preparation obligation. Unrelated tasks remain open, including a higher-priority blocking obligation. The original named-manager diagnosis case is tested on both September 1 and September 14 (before/after the September 12 deadline). Additional shared regressions exercise fixture and embedded PostgreSQL transactions, QR-to-secure-link checkout, and work added during a visit. Combined store visits retain the appropriate task projection and held-work provenance.

Evidence so far:

- `npm test`: **131 files / 890 tests passed**, normal configured worker count, 162.91 seconds. The original held-work failure and both baseline timeout cases pass in this full run.
- Seed: **15 stores, 3 regions, 5 vendors, 2 internal technicians, 100 requests, 422 work orders, 399 visits, 144 assets, 222 PM occurrences**; separate 65-store fixture validated.
- Real HTTP cookie-container test preloaded the old facilities/accountability `/app` cookies, then selected store-manager/complete. Page and API headers both contained only the new selections. Export returned **Store 104**; facilities-only accounting endpoint returned **403**. This did not manually inject the role header into the API requests.
- Session/command regression uses the actual session loader and API command with fixture persistence: selected regional membership is the audit actor; Store 104 scope and facilities-only restrictions are enforced.
- Browser: switched facilities → store manager, observed Store 104 scope and denied work creation under its configured permissions; switched back to facilities and observed the creation form. Manager search for 104 still returned 81 matches across five types. No browser-created service/store/work record and no location permission request.
- README fixture counts and status links updated. The dated review remains historical evidence.

Final typecheck, lint and build passed; `test:e2e` passed **4 files / 52 tests**. All six required commands passed for this pass. Private-preview publication succeeded after the populated-upgrade repair described below. The full browser mutation matrix remains P7-03; domain/database journeys do not substitute for it.

### Pass 1 handoff — historical starting point for Pass 2

Work together on `operator-presenter.ts` dashboard cohorts, `operator-query-presenter.ts` filter labels, loader query normalization and repository filter implementations. Pending requests need a combined submitted/under-review filter carried into query-first lists. Vendor-response stages must use the same source predicate across shared/store/regional/facilities dashboards and both list paths. Inspect all occurrences of onsite-vendor labels, including store summaries. Reconcile the lifecycle spotlight with the exact cost/history destination before changing its label. Add record-ID equality assertions across presentation and repository queries, not only URL/string assertions.

### Publication repair — P6-08, complete

Private version 28 from commit `f076dc08cba4b43b81d49d71be7886ab8e8c151e` failed with `FOREIGN KEY constraint failed: SQLITE_CONSTRAINT (extended: SQLITE_CONSTRAINT_FOREIGNKEY)`. Site: `appgprj_6a733d7dbb708191a9b80f3f9424582a`; version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_3e74728869f88191b5b0e5ff58bc6e80`; deployment: `appgdep_6aa8673ca87c8191b0b7edbd8c0c5a1e`. Recent worker logs contained no additional errors.

The new populated migration regression reproduced that error at `0039_uneven_morph.sql`: existing work verifications reference the visit-work table being rebuilt. Empty-chain and work-order-only upgrade tests missed it. The repair preserves verification rows in a transaction-local staging table, rebuilds visit-work and its indexes, restores the original verification schema/indexes and all records, then removes staging. Foreign keys stay enabled. The populated regression now passes and checks record preservation and foreign-key integrity after each remaining migration. The populated migration tests, typecheck and lint passed after the repair. Application source is unchanged from the fully validated Pass 1 build; the deployment archive will be repackaged with the repaired migration. Private version 29 subsequently published successfully; see the final checkpoint below.

### Pass 1 final checkpoint — September 14, 2026

- **5 of 40 items complete:** P1-01 through P1-04 and P6-08. **35 remain.** Resume at Pass 2; do not skip later architectural or production-readiness gates.
- Private version **29** published successfully at **2026-09-14 21:40:30 UTC**: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Access remained owner-only; no sharing policy was expanded.
- Published application/migration source: `b5697ab50105cd9646be74d0f09c22de7345d6c1`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_c5e61f0b8e80819180bb723dea91d4d3`. Deployment: `appgdep_6aa869babddc8191aa3192afb0a018fe`.
- All six required checks passed for Pass 1: seed, typecheck, lint, full suite (890 tests), e2e (52 tests), build. After the migration-only repair, the expanded migration-chain tests, typecheck and lint passed again; the unchanged application build was repackaged with the repaired SQL. The full suite/e2e were not repeated after that SQL-only change.
- P6-07/P7 remain final-release gates after the remaining passes, despite this successful incremental private publication. Real production identity, external delivery, recovery/load proof and the complete browser mutation matrix remain unverified.
- The working checkpoint documentation is updated after publication; no application change follows the published source. Existing unrelated `dev4.log` was preserved.

### Pass 2 — implementation and browser evidence, September 14, 2026

The standing UX rule is saved in this checklist and AGENTS.md. [The metric map](PLATFORM_PASS2_METRIC_MAP.md) inventories current dashboard counts, cost bases, windows, cohorts, destinations and their tests.

Implemented pending-request filtering in fixture and SQL repositories, bounded stage queues, shared plain-language work statuses, task-owned vendor waits, complete chart groups, whole-equipment spotlight cost parity, role-filtered attention counts, distinct invoice-record counts, and exact PM closed-window evidence. Phone overview now uses a compact two-column metric layout, readable supporting text, stacked section headings/actions and a high-contrast primary button. Repeated chart-footer instructions and unnecessary dashboard prose were removed.

Browser findings changed the implementation: the broad vendor state contained a declined job and an operator's date decision, so the final vendor-response cohort follows the primary typed task and active assignment. It contains **2** jobs, not the initially corrected status-only count of 4. The matching status category is labelled **Vendor follow-up**. A store-manager count included an inaccessible task; visibility now precedes counting. The unfiltered invoice page lacked the existing role guard used by filtered evidence; both paths now reject unauthorized roles before reading invoice data. This narrow fix does not complete the broader P4-02 authorization audit.

Local browser evidence:

- Pending tile opens exactly **5** requests with the selected filter visible. Vendor tile opens **CPS-2026-0116 and CPS-2026-0206**; approved unsent work remains a separate **14-job** queue.
- Overview shows all **9** work statuses totalling **34** open jobs. Three active visits are labelled as visits; the vendor grouping shows two companies. Work follow-ups opens the full **53-item** task/follow-up cohort.
- Store 115 review retains whole-equipment scope, Aug 25, 2025–Aug 25, 2026, **$440** and two supporting cost lines. Repair price, approved replacement and planning estimate remain separate.
- Finance's **90 Invoice records** link opens the **90-invoice** register. Switching to Store manager on that URL now displays the intended access message. Store 104 dashboard and the manager search (**81 matches across five types** for 104) remain scoped and connected.
- Closed PM evidence shows **92 completed / 99 eligible**, including **7 missed**. Window filters persist through status/view/page links; program/store/region context remains attached.
- Visually inspected Overview at **390 px**, vendor queue and public visit forms at **375 px**, and desktop at **1280 px**. Overview fits the viewport and has no document-level horizontal overflow. Inspected store creation and deferred work routing forms, vendor authorization, no-WO option, and the Store 112 checkout link showing the same QR-started active visit. These were inspections, not service/store creation or checkout submissions; P7-03 remains open.

Validation is finishing. The first full run passed 903 tests. A later full run caught a PostgreSQL timestamp-ordering incompatibility in the new vendor filter; it was fixed with portable null ordering, and all 15 PostgreSQL integration cases passed afterward. A transient duplicate local variable in the invoice-page edit was fixed and its two route-boundary tests passed. Final required command results and publication status will be recorded below.

Pass 3 should start with P3-01/P3-02 defaults and routing, then the work-record header and remaining mobile/forms copy. P3-03 is only partially addressed by this pass's Overview improvements; public/store forms and broader responsive polish still need their dedicated pass. Keep all later IDs and production gates open.

### Pass 2 validation checkpoint

All six required checks passed on the final application source: seed; typecheck; lint; **133 files / 905 tests** in the full suite (129.66 seconds); **4 files / 52 tests** in test:e2e (55.09 seconds); and production build. The Sites Node build wrapper failed to resolve npm on Windows before starting the application build; direct `npm run build` then completed successfully. The build reports duplicate emitted CSS filename warnings and static route-classification limitations; these did not fail the build.

**P2-01 through P2-06 are complete. 11 of 40 items are complete; 29 remain.** The full browser submission matrix and final production-readiness gates remain open.

### Pass 2 publication — complete

- Private version **30** published successfully at **2026-09-14 22:25:01 UTC**: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Owner-only access was preserved.
- Published source: `2f6cd3e0353ffd307930e4259b564fbe751f221e`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_a1bf4c83142c819185d7ff17acbd9451`. Deployment: `appgdep_6aa8742fe62c81918584881f47d78ff2`.
- The Node packaging wrapper could not locate bash in the Windows environment. Running its unchanged packaging script through the installed Git Bash succeeded; archive validation confirmed the worker entrypoint, hosting manifest and migrations before saving. No application source changed after validation/publication.
- This post-publication checkpoint is documentation only. Existing unrelated `dev4.log` was preserved. Resume with **Pass 3**, retaining the standing UX rule and all 29 remaining IDs.


### Pass 3 — implementation in progress

Grouped work: basic creation/routing and recoverable forms (P3-01/P3-02), one accountable work header and restored attention rows (P3-04/P3-05), then mobile/public forms and browser journeys (P3-03/P3-06/P3-07). The standing UX rule remains mandatory. Initial browser checks created basic, internal and outside-vendor work and a temporary store in the local process only. Two old assertions in the first full run expected removed explanatory copy and absent defaults; those expectations have been updated. Browser creation exposed missing address_1/address_2 hydration in the fixture adapter; fixed with read-after-create coverage. Browser visit entry exposed the need for an explicit location-decline action. No item is complete until final checks and browser evidence are recorded.

### Pass 3 implementation and browser evidence — September 14, 2026

- **Basic creation and routing (P3-01/P3-02):** store and problem create routine, unclassified work with Choose later selected in both UI and API. Optional classification and service details stay collapsed. Internal routing asks for a named technician only when selected; vendor routing searches friendly names, specialties, aliases and coverage. Pricing requests and future-visit approval remain secondary choices. Added command/API coverage for missing internal assignees, the internal-to-vendor handoff on one WO, and idempotent minimal creation. Existing issuance tests preserve operator references and reject responses against superseded assignments or revisions.
- **Work record and manager overview (P3-04/P3-05):** every work tab retains one prominent state, next action, accountable party, due time and escalation header. Assigned internal work names the technician. Detailed stage evidence and history moved into their tabs; price-entry controls stay in Prices & costs. The primary action uses the actual workflow destination. Facilities and regional home now show actionable attention rows before the larger charts; the six primary destinations remain. Regional browser selection showed Central District / five stores with its own 17 review items, eight open WOs and one active visit. Facilities view was restored afterward.
- **Phone and form states (P3-03/P3-06):** reviewed 375/390 px store, creation, work-record and public-visit layouts plus a 1280 px work-record/overview view. Headings and actions stack, inputs remain readable, and primary controls have touch-sized height. Shortened store/technician/vendor instructions and removed repeated explanatory panels. Reviewed the shared loading, empty, error and partial-state components and their action destinations. Native validation blocks a missing internal assignee; saving disables duplicate submission; a rejected cross-store asset save focuses an alert and retains store, problem and asset entries. A new empty store shows zero counts and optional setup; the store-manager role correctly denies facilities-only creation.
- **Store creation (P3-07):** browser-created P3-901 exposed the fixture adapter's missing numbered address fields. After the fix, P3-902 saved `902 Example Way, Suite 2, Demo City, KY 40001`, reached optional equipment setup, and Skip for now opened the complete store record. Automated read-after-write coverage includes both address lines and aliases.
- **Cross-channel / no-WO journey (P3-07):** browser check-in at Store 104 selected No work order, BrightLine, a technician name and a short service reason. Continue without location recorded an explicit decline. QR entry created visit `visit-3e3a5e35-8e92-42da-8459-213c2300dfaf`; its secure checkout link resumed the same active visit. This exposed stale client state across token navigation, fixed by keying the visit component by token. Checkout recorded Waiting on parts, a return-visit note, original arrival/departure and QR-to-SecureLink provenance. The operator then created and linked **CPS-2026-2285**, preserving the original visit and creating its required follow-up. No verified time was backdated.
- **Reconciliation recovery:** an injected failure after WO creation leaves one saved WO and the original unmatched visit. The redirect supplies a direct recovery link with the saved WO preselected. Retrying the same submission links the original records; a third replay creates neither another WO nor another reconciliation audit event. Visit counters and No work order filter labels now use the same linked-work predicate, including multi-job visits.
- **Other browser checks:** minimal/deferred work **CPS-2026-2282**, internal **2283**, outside vendor **2284** and visit-derived **2285** were actually created. Vendor search for “beer cave” found ColdLine. Manager search for 104 returned 83 matches after the local WO/visit additions; its nine-open-work drill-down showed nine Store 104 records. The public vendor link opened response choices and recorded opened status; no acceptance, date proposal, email or SMS was sent. Invoice SUM-104-2611-W retained its $8,900 billed amount, warranty-review facts and human-decision restriction. PM closed-window view showed 92 completed of 99 with seven missed; lifecycle showed three current cases with separate repair and replacement figures. Invoice/PM/lifecycle were inspection only.

All browser-created stores, WOs and visits were in the disposable local in-memory process. Presentation seed remains exactly **15 stores / five vendors / two internal technicians**; no test record is part of the published seed. Browser checks here are manual evidence, not the repeatable browser suite reserved for P7-01. Production identity, bounded-query extraction, delivery infrastructure and final release acceptance remain in their original later passes.

### Pass 3 validation — complete

All six required checks passed: `db:seed`, `typecheck`, `lint`, **133 files / 910 tests** in the full suite (154.14 seconds), **4 files / 52 tests** in `test:e2e` (57.27 seconds), and production build. The final application change removed an unreachable duplicate filter-label branch found by lint; the subsequent lint run passed. Earlier runs exposed outdated copy/default expectations, which were updated alongside the shorter UI. The Sites Node build helper again failed to resolve npm on Windows; direct `npm run build` succeeded. Existing non-fatal duplicate CSS-output and static route-classification warnings remain.

**P3-01 through P3-07 are complete. 18 of 40 items are complete; 22 remain.** Keep the UX-first standing rule in every later pass. Begin Pass 4 with production identity isolation, active membership resolution and organization-first adversarial checks; do not interpret the fictional role picker or this successful preview as production identity proof. Private publication status is recorded separately below.

### Pass 3 publication — complete

- Private version **31** published successfully at **2026-09-14 23:04:30 UTC**: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Owner-only access was preserved.
- Published source: `a2a28adab0bcc90a0e70f5a03693a26fe0831043`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_8af1148b4558819187bcd9a9d0f21e72`. Deployment: `appgdep_6aa87d6e9e288191834f7590a347f769`.
- The unchanged packaging script succeeded through installed Git Bash after the Node wrapper could not start bash. The archive included the server entrypoint, client assets, hosting manifest and existing migrations. D1/R2 bindings and presentation seed were preserved.
- The local preview process was stopped, discarding its temporary browser-test records. The temporary browser viewport was reset. Existing unrelated `dev4.log` remains untouched. This post-publication checkpoint changes documentation only; resume at **Pass 4** with all 22 outstanding IDs retained.

### Pass 4 implementation and browser evidence — September 14, 2026

Grouped work: identity/membership and clear access screens (P4-01); organization-first loader/repository boundaries and adversarial scope tests (P4-02); shared mutation guards and existing transactional regression proof (P4-03); public capability/visit regression checks (P4-04). The standing UX priority applies throughout. Configuration and provisioning are documented in [Operator access](OPERATOR_ACCESS.md).

- **Identity:** deployed runtimes fail closed unless explicitly configured. Authenticated Sites identity resolves by exact verified subject to an active database user and one active membership in the selected organization. Suspended users/memberships, no scopes and ambiguous memberships deny access; preview cookies never select authenticated roles. Organization/division/region/store grants resolve through scoped repository queries, including the PostgreSQL adapter. Render does not trust Sites headers. Demo startup seeding runs only in preview. Real-user onboarding and a Render identity provider are not implemented or claimed.
- **Data boundaries:** removed fixed-demo-organization and omitted-organization snapshot calls; local snapshots now filter tenant rows/users. Existing SQL organization predicates and store scopes remain. Empty explicit scopes deny access. All operator mutation routes now pass the actual request through the shared origin/read-only guard. Mixed read/write scopes remain read-only; company setup and operations spanning stores require companywide access. Preview accounting/role/package mutations are disabled outside preview. CSV/report routes return explicit access errors. Preview audit names now identify the same fictional persona as the membership actor.
- **Domain and capability evidence:** new tests cover foreign-company and empty-scope search/list/detail/aggregate reads; missing sign-in on reads/commands/exports; narrow-store denial; mixed read/write denial; six public capability types with expiration, revocation, wrong purpose and foreign-tenant subjects. Existing regression suites cover quote/private-file denial, vendor/work eligibility, immutable issuance revisions, append-only report/response/evidence/cost/invoice/deadline records, stale versions, transaction rollback with audit/outbox, idempotent operator/public retries, multi-WO atomic check-in/checkout and one required unresolved follow-up. No independent mutation implementation was added to the UI channels. One-time geolocation remains check-in/out only.
- **Browser evidence:** on the local fixture, actually created **CPS-2026-2282** from Store 101 and “Pass 4 local access check: leaking tap”; it saved Routine / Choose later / Classification deferred with an accountable action and due time. Pending-request drill-through showed exactly five records; Ridgeview search returned 77 matches across five record types. Inspected Add a store, PM (222 occurrences), lifecycle (three current cases), invoices (90 records), vendor authorization and 375px technician/no-WO selection. No-WO selection exposed vendor/reason fields. These latter workflows were inspection only in this pass; the prior pass records full store creation and QR-to-secure-link checkout submissions. Current automated suites re-exercised those shared commands.
- **Authenticated browser evidence:** temporarily configured the local worker through ignored `.env.local`; parent-shell variables alone did not configure its Cloudflare environment. `/app/overview` then returned 307 to the short sign-in page, with no operator records or preview switchers. Inspected 375px Sign in and Access needed, plus 1280px Choose your company. Submitting a company without identity returned to Sign in. No real login or real membership was created. Temporary configuration was removed and the local server stopped.
- **Early failures corrected:** initial full-suite failures were six old mock assertions after adding the request guard argument; updated those assertions. The new PostgreSQL membership test initially ran before the seed test; moved it after seeding. Both issues are fixed, with subsequent full-suite and workflow runs passing.

### Pass 4 validation checkpoint

All six required checks passed: seed, typecheck, lint, **135 files / 932 tests** in the full suite (141.14 seconds), **4 files / 53 tests** in the workflow suite (52.74 seconds), and the production build. Targeted boundary/PostgreSQL checks also passed **3 files / 37 tests**. The Sites Node build wrapper again failed to resolve npm on Windows; direct `npm run build` succeeded. Existing duplicate emitted-CSS and route-classification warnings remain nonfatal. The new checks preserve exactly 15 presentation stores and five outside vendors, with the separate 65-store fixture.

**P4-01 through P4-04 are complete at the implemented application-boundary level. 22 of 40 items complete; 18 remain.** Real-user onboarding, a Render identity adapter, external infrastructure, recovery/scale evidence and final acceptance remain separate gates. Private publication is pending; the hosted showcase will explicitly retain `OPS_ACCESS_MODE=preview` (environment revision 1), with existing owner-only access unchanged.

The additional `npm run build:render` portability build passed. Temporary browser sizing was reset. All Pass 4 browser mutations were local, and stopping the owned preview discarded them. `dev4.log` remains untouched. Resume Pass 5 by inventorying snapshot consumers before replacing the dashboard and record queries; preserve all exact-cohort and tenant-boundary regression checks.

### Pass 4 publication — complete

- Private version **32** published successfully at **2026-09-14 23:46:34 UTC**: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Owner-only access and the D1/R2 bindings were preserved. Environment revision **1** explicitly retains the fictional preview mode.
- Published source: `9fc6320daf48d251bd4bd78f961469571d16d08f`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_85df7238cbd88191bc9060cee56d42ba`. Deployment: `appgdep_6aa8874d0cc08191b1800243723a4230`.
- The unchanged packaging script succeeded through installed Git Bash after the Node wrapper could not launch bash. Validated the server entrypoint, client assets, hosting manifest and migrations before saving. No application source changed after validation/publication; this checkpoint is documentation only.
- Resume at **Pass 5**. **22 of 40 complete; 18 remain.** Keep the standing UX rule and the explicit production-rollout limitations in Operator access.

### Automatic continuation — authorized September 14, 2026

The user requested that the remaining passes continue automatically until done while away. An active goal and hourly follow-up named **Finish platform improvement passes** are attached to this task (automation ID `finish-platform-improvement-passes`). Continue implementation without asking for another pass prompt. Disable the follow-up when work is complete or only documented external blockers remain; do not repeatedly retry unchanged infrastructure blockers. No authorization to contact external recipients, invent credentials, create a Render project, or broaden the private Site audience was given.

Pass 5 has started with the snapshot/adapter inventory. `operator-loader.ts` still funnels roughly thirty dashboard, record, program and setup consumers through `sessionAndFixture`; several API/setup loaders also use full snapshots. Some setup pages load a snapshot only to obtain session access and can drop that read immediately. Normal lists/search already use query-first paths; specialized filters and Trends still use compatibility projections. PostgreSQL currently imports the repository from `d1-repository.ts` and emulates D1 prepared statements through a SQL translator. Refactor a genuine provider-neutral query/execution boundary before adding new feature queries; preserve native transaction behavior and the existing 932-test baseline. Keep stable item IDs and leave P5 items unchecked until their complete acceptance evidence exists.

Next work groups: (1) inventory and remove needless setup reads, establish the neutral persistence boundary; (2) scoped dashboard aggregates and exact source pages; (3) bounded record/program/lifecycle queries and feature extraction; (4) audited legacy retirement and import/fixture proof. Then continue Passes 6–7, recording actual deployment/recovery/scale dependencies separately from local evidence. Last published application remains private version 32; no application change has been made since that publication.

### Pass 5 — persistence foundation validated

The current [query inventory](PASS5_QUERY_INVENTORY.md) retains every remaining dashboard, list, program, record, creation, governance, vendor and API snapshot consumer. Notification settings and import-workspace access no longer load source snapshots; a regression test rejects any such read.

For P5-04, the shared repository now uses `OpsSqlDriver` (parameterized queries, affected rows and atomic execution). D1 objects remain in the small Cloudflare adapter. PostgreSQL directly uses its native pool/client, including reuse of an existing transaction without nesting or releasing it. Removed the D1 prepared-statement emulator and SQLite-to-PostgreSQL SQL translator. Repository construction explicitly selects native JSON/string-aggregate expressions; parameter binding handles numbered placeholders and schema booleans only. Seed/job inserts now use `ON CONFLICT DO NOTHING` on both engines, and the work-order counter expression is explicitly qualified in its source SQL. No migration or hosting-binding changes.

Initial engine/seed checks passed **4 files / 28 tests**. The expanded full suite passed **135 files / 935 tests** (136.01 seconds), including real migrated SQLite and embedded PostgreSQL checks for native taxonomy/cost queries, tenant predicates, transaction behavior, one-time job/outbox claims and saved-view deletion. The workflow suite passed **4 files / 53 tests** (53.90 seconds). Seed remains 15 stores, five vendors and two internal technicians; the separate 65-store fixture remains. Final typecheck, lint, Sites production build and Render portability build passed. The Sites Node build wrapper failed to resolve npm on Windows; direct `npm run build` completed successfully. Existing nonfatal CSS-output and route-classification warnings remain. **P5-04 is complete: 23 of 40 items complete, 17 remain.** This is an incremental checkpoint, not completion of Pass 5 or the overall goal.

Browser inspection confirmed notification settings/import access, the 15-store dashboard with 61 review items / two vendor responses / three active visits / $216,737 recorded work cost, exact five-request drill-through, store/work creation forms, PM/lifecycle, 90 invoices, vendor authorization and secure-link resumption of the QR-started Store 112 visit. The import screen fits 375px. These checks were inspection only; no data was mutated or external message sent. P5-01/P5-02/P5-03/P5-05/P5-06 remain unfinished.

### Pass 5 persistence publication — complete

Private version **33** published successfully at **2026-09-15 00:09:31 UTC**, from source `74fb76ad2df8bc8b883e6c2abe6d3d87fddce9d2`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_01217ab31190819187ddecfe5aea21e1`; deployment: `appgdep_6aa88cad7c34819190bd1893cfa0d448`. Owner-only access, D1/R2 bindings and environment revision 1 were preserved. The unchanged packaging script ran through installed Git Bash after the Node wrapper failed to start bash; archive validation confirmed server/client output, manifest and migrations. Temporary browser sizing was reset. `dev4.log` remains untouched. Continue the remaining Pass 5 items automatically; this publication does not complete the overall goal.

### Pass 5 legacy retirement — validated

The [retirement audit and file manifest](PASS5_LEGACY_RETIREMENT.md) records the syntax-tree import walk from 199 runtime/configuration/script roots, 420 reachable files, zero unresolved local imports and zero nonliteral dynamic imports. Removed 93 unreachable prototype files / 50,070 lines, including eight legacy-only test files. The current `ops-*` tests, active routes, domain, migration histories and hosting inputs remain. No application route was redirected or deleted. Historical reviews remain dated; README links the current retirement record.

Seed, typecheck, lint, **127 files / 884 tests** (132.98 seconds), **4 files / 53 workflow tests** (56.26 seconds), Sites production build and Render portability build passed. The test total decreased by 51 because those tests covered retired implementations; no current-platform test was removed. The read-only local HTTP route audit passed **148 sampled routes**, with no HTTP or rendered-server errors; this excludes client hydration and is not the final browser suite. **P5-05 is complete: 24 of 40 items complete, 16 remain.** Browser reload preserved the dashboard and manager search returned 81 matches across five record types; the Store 104 open-work link led to eight work records with Store 104 / Open work filters. No browser data mutation was made during this retirement checkpoint.

Resume the remaining query work at the **Next query group** in `PASS5_QUERY_INVENTORY.md`. The existing executive snapshot is not a drop-in overview replacement because its time/cohort and task semantics differ. Keep P5-01/P5-02/P5-03/P5-06 and every later gate open.

### Pass 5 retirement publication — complete

Private version **34** published successfully at **2026-09-15 00:19:30 UTC**, from source `fd6f844ed25a4aa7776e6cd4e4393fc0b950ce20`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_5a93a2c7b55c8191ae6229a1dd183466`; deployment: `appgdep_6aa88f03df708191a14202c69b253c68`. Owner-only access, D1/R2 and environment revision 1 were preserved. The exact pushed source was packaged with the installed Git Bash helper; server/client output, hosting manifest and migrations were validated before saving.

**Current checkpoint: 24 of 40 complete, 16 remain.** Pass 5 and the overall goal remain active. Continue P5-01/P5-02/P5-03/P5-06 automatically, then Passes 6–7. The local preview at port 3000 is retained for the continuing query/browser work; its data remains the original 15-store in-memory fixture. No temporary viewport override is active. The only unrelated untracked file is `dev4.log`; preserve it. This post-publication checkpoint changes documentation only.

### Pass 5 dashboard query foundation — validated

Implemented native scoped activity aggregates and bounded store/category/month/status/vendor group pages, with independent current-state versus service-date windows, explicit currency, unclassified costs, zero-cost groups, stable cursor ordering and totals on empty pages. Shared organization/scope and vendor-response predicates now serve both existing queues and these queries. Extracted the five-role dashboard renderer into `dashboard-presenter.ts`; it accepts aggregate values and an independently counted seven-row review preview. The screen keeps its existing wording and destinations.

The [query inventory](PASS5_QUERY_INVENTORY.md) records the remaining attention/lifecycle query work. **The live dashboard loader still uses its snapshot adapter; these native aggregates are not yet connected to it.** P5-01/P5-03 remain unchecked, and the checkpoint stays **24 of 40 complete / 16 remaining**.

The first PostgreSQL run found a date-to-month cast mismatch; corrected the native expression and reused the existing source-cost filter. The subsequent PostgreSQL/fixture run passed 2 files / 19 tests; presenter/edition/metric checks passed 4 files / 50 tests, and the final role/seven-row check passed 2 files / 22 tests. Final validation passed all required commands: seed, typecheck, lint, **128 files / 888 tests** (134.67 seconds), **4 files / 53 workflow tests** (60.79 seconds), Sites build and Render portability build. The Sites Node build helper failed to resolve npm on Windows; direct `npm run build` succeeded. Existing nonfatal CSS-output and static route-classification warnings remain.

Browser inspection verified all five role dashboards: facilities 61 review items / two vendor responses / three active visits / $216,737 recorded cost; executive 34 open jobs / eight watch assets; finance 90 invoices / 273 cost-bearing WOs; regional eight open jobs / 17 review items / $64,315; Store 104 eight open jobs / 25 visits / $37,443. The pending-request tile opened the five expected requests with the pending filter. Phone screenshots at 375px preserved clear controls and readable layouts. Role changes only affected local preview cookies; no source record or external delivery was mutated. Facilities preview and normal viewport sizing were restored.

### Pass 5 dashboard foundation publication — complete

Private version **35** published successfully at **2026-09-15 00:43:27 UTC**, from source `c6c5b2a6bd5cc7d84d6bf284e729fad07984f314`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_affaa209e19081919ffde2fadc04fef2`; deployment: `appgdep_6aa894a1e6c88191b05e6b6191744899`. The owner-only audience, D1/R2 bindings and environment revision 1 were preserved. The exact pushed source was packaged and validated before saving; installed Git Bash ran the unchanged packaging helper after the Node wrapper could not start bash.

The overall goal remains active, with **24 of 40 items complete / 16 remaining**. Continue automatically from the attention-query instructions in `PASS5_QUERY_INVENTORY.md`, then lifecycle evidence and the live dashboard loader. Do not label P5-01/P5-03 complete while the live loader still reads a tenant snapshot. Local port 3000 remains available for this continuing work, normal viewport sizing is restored, and `dev4.log` remains untouched. This publication checkpoint is documentation only.

### Pass 5 connected attention and dashboard totals — validated

The live home loader now obtains current activity/stage aggregates and a seven-row attention preview from the repository. The native attention query counts all six source populations after organization/location, role, target-route and edition filtering; it preserves task/follow-up deduplication and grouped quote source counts. It supports active lane/group filters, full totals on empty pages and stable keyset pagination. UTC-normalized deadlines avoid duplicate rows when equivalent timestamp formats meet a page boundary. No full source history is returned for a dashboard row; its record link opens the evidence. The complete action-center list retains its existing expanded sources and snapshot path for now.

Removed the redundant intermediate dashboard builder and repeated queue/lifecycle calculations. The dashboard renderer uses the new activity values for metrics/stages and independent review totals. Shared exception labels remain consistent; quote explanations are shorter. **Chart, lifecycle and invoice spotlight inputs still use the tenant snapshot. P5-01/P5-02/P5-03 stay open, with 24 of 40 items complete and 16 remaining.** The next steps are recorded in `PASS5_QUERY_INVENTORY.md`.

Validation passed seed, typecheck, lint, **129 files / 894 tests** (161.80 seconds), **4 files / 53 workflow tests** (60.50 seconds), Sites production build and Render portability build. Shared SQLite/PostgreSQL regressions cover five roles, both editions, scoped/grouped counts and pagination. A separate migrated SQLite fixture adds 225 same-deadline obligations and proves complete traversal, unique identities and at most page-size-plus-one returned rows. Five renderer tests fail if prepared dashboard values trigger another attention projection. Test fixture setup initially needed its copied tasks to be nonblocking and its SQLite seed bindings to normalize booleans; both were corrected before the successful checks. The Sites build helper again could not resolve npm on Windows; the direct production build passed.

Browser inspection confirmed all five dashboards: facilities 61 review items, two vendor responses, three active visits, 53 work follow-ups and $216,737 recorded cost; executive 34 open jobs/eight watch assets; regional eight open jobs/17 review items/$64,315; Store 104 eight open jobs/25 visits/$37,443. The facilities 61-item tile opened the 61-item review queue. The 375px phone view retained readable controls, summaries and actions. Browser changes were preview-role cookies only; no source data or external delivery was changed. Facilities and normal viewport sizing were restored, and the ongoing-work tab was retained.

### Pass 5 attention publication — complete

Private version **36** published successfully at **2026-09-15 01:07:12 UTC**, from source `7e403502bac16ba780955e9a1b50ead61c632209`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_17b33a9703848191949a8e2bad20e83e`; deployment: `appgdep_6aa89a33c60c81919381355884d0d05e`. The owner-only audience, D1/R2 and environment revision 1 were preserved. The unchanged packaging helper ran through installed Git Bash after the Node wrapper could not start bash. Archive validation confirmed the `dist/` root with server entrypoint, client assets, manifest and migrations before saving the exact pushed source.

**The overall goal remains active: 24 of 40 complete / 16 remaining.** Continue automatically with paginated chart presentation and lifecycle/estimate/spotlight evidence as recorded in `PASS5_QUERY_INVENTORY.md`; then retire the dashboard snapshot before marking P5-01/P5-03 complete. The full action center still needs its complete filter/history/source-detail query migration. Keep Passes 6–7 and every external operational-readiness gate open until proven. Port 3000 remains available for the continuing work, no temporary viewport override is active, and `dev4.log` is untouched. This post-publication checkpoint is documentation only.

### Pass 5 connected charts and store cost ranking — validated

All home chart groups now come from bounded repository queries. Store and vendor charts show five rows, category charts retain an explicit Unclassified row, work statuses retain all current states, and the trend uses twelve months. Full-cohort totals and percentages remain independent of the visible rows. Short rankings label their coverage and open exact source records with the service-date window and currency preserved. The 15-store overview now links to a dedicated, query-backed store cost ranking with 25-row pages and server-side name/number search. Zero-work stores remain visible. The ranking/report compatibility default is the same rolling 12 months, and scoped/invoiced median comparisons no longer jump to an unrelated all-history ranking.

UI polish includes readable 15px chart labels/amounts, 13px shares, compact organization-neutral location labels such as “Store 104 · Ridgeview,” full readable vendor names, zero-length bars for zero values, a table ranking, and removal of the redundant generic metric introduction on that screen. Removed the old snapshot trend builder and snapshot chart construction. **Lifecycle/estimate and invoice spotlights still keep the home loader on its tenant snapshot. P5-01/P5-02/P5-03 remain open; the checklist stays 24 of 40 complete / 16 remaining.** The next lifecycle inputs and domain functions are identified in `PASS5_QUERY_INVENTORY.md`.

Seed, typecheck, lint, **130 files / 897 tests** (133.78 seconds), **4 files / 53 workflow tests** (84.59 seconds), Sites build and Render portability build passed. Shared migrated SQLite/PostgreSQL checks cover scoped totals, source-record parity, search escaping and pages. A separate 65-store migrated SQLite fixture proves all zero-work stores remain reachable across three pages. The first zero-work store join made the targeted run slower; aggregating store costs before joining restored the native two-file run to 54.90 seconds. These timings are local checks, not production-scale certification. The existing Windows Sites helper fallback was used; no hosting bindings or migration history changed.

Browser inspection confirmed the owner overview’s five-of-15 store chart with a $216,737 full total, short location names and readable vendor rows. The complete ranking kept September 1, 2025–August 25, 2026 / USD, showed all 15 stores, and opened Store 104’s $37,443 amount into 21 exact cost-bearing work records with the same filters. Its phone search returned one matching Store 104 while preserving the period. Small copy/layout refinements were rechecked in the browser and final builds. Facilities preview and normal viewport sizing were restored. No source data or external delivery was mutated; `dev4.log` remains untouched.

### Pass 5 charts and store ranking publication — complete

Private version **37** published successfully at **2026-09-15 01:38:11 UTC**, from source `c011a0b20e619e7d6dc3ca7275da6f963fecbb24`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_6514c0e0a76c8191a81d478bb8b751c0`; deployment: `appgdep_6aa8a1741ac881918acda32d8976110b`. The owner-only audience, D1/R2 bindings and environment revision 1 were preserved. Both final build logs reached their successful completion; packaging through the unchanged Git Bash helper succeeded, and the archive was validated for server entrypoint, client assets, manifest and migrations before saving the exact pushed source.

**The overall goal remains active: 24 of 40 complete / 16 remaining.** Continue automatically with the remaining lifecycle/estimate, equipment/invoice spotlight and first-store inputs in `PASS5_QUERY_INVENTORY.md`, then remove the home loader’s snapshot dependency. Activity, attention and charts are already connected. Full action-center, record/program/Trends migration and Passes 6–7 remain open; no infrastructure or final-release gate is implied by this checkpoint. Local port 3000 and the browser tab remain available, normal viewport sizing is restored, and `dev4.log` remains untouched. This post-publication checkpoint is documentation only.

### Pass 5 bounded invoice and store context — validated

The live dashboard now reads its store shortcut and invoice spotlight through `getDashboardContext`. Each native query returns at most one record. Invoice selection uses the oldest open review evidence and a stable invoice ID, normalizing equivalent timestamp formats. The complete invoice must be visible: at least one allocation, all allocated stores inside the organization/location scope. Activity counts and the spotlight share that predicate. The presenter receives only the necessary invoice/store fields, keeps the exact invoice link, displays its actual currency and minor-unit amount, and uses shorter copy with “Needs review.”

Seed, typecheck, lint, **131 files / 898 tests** (190.17 seconds), **4 files / 53 workflow tests** (113.73 seconds), Sites build and Render portability build passed. Shared fixture/migrated SQLite/embedded PostgreSQL checks cover the new context. A dedicated migrated SQLite test exercises scoped and cross-store invoices, missing allocations, resolved/foreign flags, timestamp ties and CAD amounts while observing at most one row returned by each of two reads. Its initial seed required a separate synthetic organization for the foreign flag; that fixture correction passed. Final builds and browser inspection include the small “Needs review” wording refinement. Build/package Node wrappers hit the previously documented Windows limitations; normal build and unchanged Git Bash packaging helpers are the established fallback.

Browser inspection verified the finance overview’s invoice COLDLINE-202603-104-4, $14,725.00 USD, and exact record destination with CPS-2026-0035 / Store 104 evidence. The phone view kept the amount, status and vendor readable. The store-manager overview retained Store 104 and its exact store shortcut. Facilities overview and normal viewport sizing were restored. No source data or external delivery was mutated.

**24 of 40 complete / 16 remaining.** The remaining home snapshot inputs are lifecycle/estimate summaries and the equipment spotlight. Continue with their bounded domain inputs as described in `PASS5_QUERY_INVENTORY.md`, then remove the home snapshot dependency. Full action-center and other record/program migrations plus Passes 6–7 remain open. Keep `dev4.log` untouched.

### Pass 5 invoice/store context publication — complete

Private version **38** published successfully at **2026-09-15 01:50:05 UTC**, from source `3edf49581ee9204347fada21cd7800f711275d05`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_21e58e4c983481919f2148c8733f7163`; deployment: `appgdep_6aa8a43cf79c8191be6d73c3a6713b97`. The owner-only audience, D1/R2 and environment revision 1 were preserved. Exact source push, successful builds, Git Bash packaging and archive validation preceded saving/deployment. This post-publication checkpoint is documentation only.

The goal and automatic continuation remain active at **24 of 40 complete / 16 remaining**. Continue the lifecycle/estimate summary and equipment spotlight extraction; invoice/store context is now connected and does not need rebuilding. Port 3000 and the browser tab remain available for ongoing work, with facilities preview and normal sizing restored. `dev4.log` is untouched.

### Pass 5 main overview snapshot retired — validated

The live `loadDashboardModel` now reads its session/clock directly and calls query-specific activity, attention, charts, context and lifecycle methods. `presentQueryDashboard` renders the five roles from those outputs; the live overview no longer requests a tenant snapshot or calls the generic presenter. The remaining `buildDashboardModel` export is an explicit fixture compatibility adapter.

Lifecycle inputs stream through 100-asset batches in stable ID order. Each batch selects only the current repair work/profile/benchmark/override and grouped cost ranking evidence. The domain accumulator holds one leading candidate and currency totals. Candidate evidence reads return one latest management decision, one relevant state event, one price event, one selected quote amount and one whole-unit reported price, plus cost totals grouped by currency. Shared domain functions preserve profile matching/escalation, exclusions, age, materiality, same-horizon economics and management state. Shared price selection keeps approved amounts pinned. Current repair/replacement currencies must agree for comparison, planning totals remain separate by currency, and the equipment summary keeps the exact whole-equipment 12-month cost destination.

Final validation passed seed, typecheck, lint, **132 files / 901 tests** (157.66 seconds), **4 files / 53 workflow tests** (66.19 seconds), Sites production build and Render portability build. Shared fixture/migrated SQLite/embedded PostgreSQL queries match across scopes. A separate 370-asset SQLite fixture spans four pages, finds the leading candidate on page four, covers CAD/USD aggregates and future cost exclusion, and observes no result larger than 101 rows. The actual authenticated home-loader test throws on snapshot access and passed. The density seed initially lacked required open workflow tasks; adding those source obligations corrected the fixture. Final stable ordering changes also passed 3 files / 21 targeted tests before the final full run/builds. Existing Windows Sites wrappers failed as documented; direct build and unchanged Git Bash packaging remain the fallback.

Browser inspection confirmed all five roles: facilities 61 review items/$216,737; executive 34 open jobs/eight watch assets/three repair comparisons; regional eight open jobs/17 review items/$64,315; Store 104 eight open jobs/25 visits/$37,443; finance $3,544,394 replacement outlook. The equipment summary opened CPS-2026-0115 and whole-equipment history with the same $18,000 repair, $32,800 approved replacement and $440 recorded cost in August 25, 2025–August 25, 2026. The 375px phone summary remained readable. Facilities overview and normal viewport sizing were restored; no source data or external delivery was mutated.

**24 of 40 complete / 16 remaining.** The main overview migration is complete, but P5-01 remains open for the owner brief and its scope/source issues. Source inspection found `loadOwnerBriefModel` allows regional users yet passes only the organization ID and full organization snapshot to `buildOwnerBrief`; it also mixes cost currencies and links period metrics to broad lists. This is the next priority, documented with its exact inputs in `PASS5_QUERY_INVENTORY.md`. The companion coverage loader is executive/facilities-only and needs bounded reads as well. Do not rebuild the completed main overview. Full action-center, record/program/Trends queries and Passes 6–7 remain open. Keep `dev4.log` untouched.

### Pass 5 overview publication and access-check correction

Private version **39** published successfully at **2026-09-15 02:13:31 UTC**, from source `0468c9bbdb6c0b7dead6c9eec7ab3b32d3288894`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_d1bf12cc42b08191aeb268e0b5a92d89`; deployment: `appgdep_6aa8a9bd1f3c81918916eab595ee365b`. The owner-only audience, D1/R2 and environment revision 1 were preserved. Final source was pushed, packaged through the unchanged Git Bash helper, validated and saved before deployment. This post-publication checkpoint is documentation only.

**Corrected checkpoint: 23 of 40 complete / 17 remaining.** P4-02 is reopened because the newly identified regional owner-brief reader contradicts the prior broad access claim. Earlier 24/40 checkpoints remain historical evidence; they are not the current completion count. Resolve this with P5-01's bounded owner-brief replacement and an actual regional-loader test. Main overview query migration is complete and remains in place. The goal and automatic continuation remain active. Local port 3000, facilities overview and the continuing browser tab are retained with normal sizing; `dev4.log` remains untouched.

### Pass 5 owner brief — scoped summaries and exact source pages

The owner summary now uses eleven organization/location-scoped repository cohorts with full totals and bounded previews. Exact cost/value periods and explicit currency replace mixed-currency sums and date/timestamp comparisons. Current invoice, active-work, escalation and decision cohorts are labeled separately. Fully visible flagged invoices count once; value references must resolve inside scope. Decisions deduplicate approval/escalation tasks, preserve latest lifecycle state and remain fully pageable. PM timing uses completion/window evidence and retains every obligation bucket.

The simplified brief uses current interface tokens, readable headings and short metric notes. Its 25-row source lists stack on phones. PM rows name the plan and store and open the exact occurrence, including work without equipment, then return to the same brief filters. Store cost/work cells preserve the selected store. The nested main landmark was removed. Actual regional and store-scoped facilities loaders cannot request a tenant snapshot; the still-companywide integrity companion returns before reading data for location-scoped memberships. A call-site audit found the old companywide vendor-scorecard loader unused, so that entry point was removed rather than left callable.

Validation: deterministic seed, typecheck and lint passed. The full suite passed **133 files / 904 tests** in **138.51 seconds** after an earlier 15-second PostgreSQL test timed out under simultaneous build/test contention. The workflow command passed **4 files / 53 tests**; final PM-source and access changes additionally passed **4 files / 37 focused tests**, including migrated SQLite and embedded PostgreSQL, in **59.84 seconds**. A separate adversarial SQLite database traverses 230 same-deadline decisions with no more than 26 returned rows and verifies first-day costs, USD/CAD, conflicting references and PM timezone comparisons. Deliberately malformed test records stay outside the presentation seed. The Sites build passed; final Render validation/publication is recorded in the checkpoint that follows.

Browser checks: regional Central District shows five stores, $4,520 recorded cost and exactly ten cost lines for July 27-August 25; nine on-time PM occurrences open the corresponding store/plan records and retain the return path. Companywide figures show $12,999 recorded cost and four current flagged invoices totaling $49,925. The 375px source layout keeps amounts/dates visible. Normal viewport and facilities role were restored. The companion's truncated 'and more' rows, obsolete styles and optional-equipment cleanup wording are recorded as the next priority in PASS5_QUERY_INVENTORY.md.

**23 of 40 complete / 17 remaining.** The owner summary repair is complete, but the companywide integrity companion still uses a snapshot. Keep P4-02 and P5-01/P5-02/P5-03/P5-06 open until their full acceptance work is done. Continue automatically with that companion, then full action-center, record/program/Trends queries and Passes 6-7. Preserve the 15-store data, private audience, D1/R2 and unrelated dev4.log.

### Pass 5 owner-brief publication checkpoint

Private version **40** published successfully at **2026-09-15 02:53:18 UTC**, from source `6755ba7f59f9847fc5d66a1e133e5d4b446705ca`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_2b172b39a7c081918f12652b67f108d2`; deployment: `appgdep_6aa8b311c64081919e943e0c7a375340`. Native deployment status confirmed success with environment revision 1. The owner-only audience, D1 and R2 remain unchanged. Final Sites and Render builds both exited successfully. The Windows wrapper failures used the documented direct-build/Git-Bash packaging fallbacks; the archive was validated before saving.

**23 of 40 complete / 17 remaining; automatic continuation remains active.** Resume at the operating-integrity companion in `PASS5_QUERY_INVENTORY.md`, then the full action center and remaining record/program/Trends migrations. The main overview and owner-summary query work are complete and should not be rebuilt. Actual browser inspection is still distinct from the Vitest workflow command, and the full release matrix remains a later gate. Facilities overview is restored at localhost:3000 with normal viewport sizing; the continuing tab is retained. Source fixture data and unrelated `dev4.log` are untouched. This checkpoint changes documentation only.

### Pass 5 scoped record checks — validated

The owner brief's companion now uses twelve organization/location-scoped record cohorts and exact paginated source lists. The complete page, including both summaries, runs with tenant snapshot access forbidden in its authenticated regression test. Companywide, regional and store-scoped facilities readers share the same repository boundary. Closed work, current outcome, recorded cost and latest matching outside-work verification have independent counts. A new unfinished visit cycle cannot inherit an earlier verification. Invoice aging uses organization-bound flags and full allocation visibility.

The interface replaces the composite completion score with short Needs follow-up and Closed work evidence sections. Optional classification and equipment-life details do not make basic work invalid. Every count opens its complete source list; phone rows stack and Yes/No evidence counts remain readable. Removed the obsolete coverage helper and old styles after migrating their tests. The full action center is next, with type/urgent/search/history filters, filtered counts, supporting-source details and return-to-queue behavior preserved.

Validation passed seed, typecheck, lint, **134 files / 906 tests** in **146.81 seconds**, **4 files / 53 workflow tests** in **61.14 seconds**, Sites build and Render portability build. Shared fixture/migrated SQLite/embedded PostgreSQL checks cover all cohorts and scopes. A separate SQLite fixture traverses 230 additional tied records and exercises current/rejected verification, an unfinished newer cycle, timezone-equivalent dates and a foreign invoice flag. Returned result sizes remain bounded at 101. The initial nested correlated PostgreSQL query ran for 312.90 seconds and its identified worker was explicitly stopped; materializing current outcome/verification IDs corrected the plan, and the isolated migrated PostgreSQL check passed in 20.20 seconds before the successful full suites. This establishes query behavior, not production throughput. The Windows build/package wrappers still require the documented direct-build and Git Bash fallbacks.

Browser evidence: companywide shows 389 closed records, 387 recorded outcomes and costs, and 351 unverified outside-work outcomes across 15 source pages. Page 2 preserves the 351 total. Regional Central District shows five stores, 129 closed records, 127 recorded outcomes and costs, 115 unverified outside-work outcomes and zero aged invoice reviews; the empty source page is explicit. The 375px layout was inspected, then facilities overview and normal sizing restored. No source data or external delivery was changed.

**23 of 40 complete / 17 remaining.** P4-02 still requires the linked PM/record-reader audit; P5-01 and remaining query/production/release gates stay open. Continue automatically with the full action center in `PASS5_QUERY_INVENTORY.md`. Keep the completed overview/brief queries, fictional presentation data, private audience, D1/R2 and unrelated `dev4.log` intact.

### Pass 5 record-check publication checkpoint

Private version **41** published successfully at **2026-09-15 03:21:03 UTC**, from source `c6409eac469db4d8e0dc76b14a2c68d8b143a55c`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_ef49ea2637e48191a09bf9428a968a70`; deployment: `appgdep_6aa8b991ec988191af315ed2d0ad2fb3`. Native deployment status confirmed success with environment revision 1. Private publication preserves the owner-only audience, and the D1/R2 manifest is unchanged. Both final builds exited successfully; the unchanged Git Bash helper packaged the artifact and validation confirmed its server entrypoint, client assets, manifest and migrations before saving the exact pushed source.

**23 of 40 complete / 17 remaining; automatic continuation remains active.** Resume with the full action center. Its current snapshot entry is `loadListModel("action-center")`, the compatibility rows are in `operator-presenter.ts`, and the live UI is `ReviewQueueSurface`. The existing `listAttention` query handles the open home preview; extend the contract for queue search/type/priority/history and supporting sources rather than dropping those behaviors. Preserve filtered totals and next-item/return-to-queue paths. Broader record/program/Trends, linked-reader audit, production and release gates remain open. Local port 3000, normal sizing and facilities overview are retained; the browser tab is marked for continued work. Source data and `dev4.log` remain untouched. This publication checkpoint changes documentation only.

### Pass 5 full review queue — P5-01 complete

The live queue now uses a dedicated presenter and native scoped queries for type, priority, search, store and task-history views. Full filtered counts drive exact metric destinations; short filter labels replace unrelated whole-queue counts. Open items retain deadline/priority ordering, while completed/canceled tasks use normalized end timestamps and stable IDs. Twenty-five rows render per page; a bounded 25-item look-ahead preserves next-item hints across the page boundary. Next-item selection rechecks those IDs against current access and filters instead of trusting the hints or reloading only the old page.

Grouped tasks and quote rounds now open bounded supporting-record pages with full totals, exact anchors, owner, deadline and completion condition. Parent access is checked first; follow-up/approval/proposal references must match their organization and work/store/vendor relationship. Missing or conflicting references do not inflate counts or reveal unrelated records. Source pages retain the filtered return path and name the work/store. Simple sources reuse the exact authorized row. Underlying work/request/exception record pages remain P5-02 work; this completion does not claim those readers migrated.

The UX uses shorter store names, fewer repeated instructions, readable phone rows and 44px supporting-record links. Browser checks confirmed 61 companywide open items, 13 for search 104 and one after Do now; the warranty source retained both filters. History showed 97 records across four pages, including verified page two. The quote round exposed two requests and two versions. Central District showed five stores and 17 items; changing from facilities to regional on the Store 105 quote source showed the explicit unavailable state. The phone queue and source page were inspected at 375px. Facilities overview and normal sizing were restored; no source data or external delivery was mutated.

Validation passed deterministic seed, final typecheck/lint, **135 files / 908 tests** in **152.40 seconds**, **4 files / 53 workflow tests** in **62.93 seconds**, Sites build and Render portability build. Final search-responsibility wording and source-context refinements passed **4 focused checks** (35 unrelated tests skipped) across authenticated loader, adversarial SQLite, migrated D1/SQLite and embedded PostgreSQL in **22.27 seconds**. The density fixture traversed 230 completed tasks with equivalent timestamp representations and 230 quote revisions, keeping returned pages at most 26 rows for 25-row requests. The deliberate malformed-reference amendment initially failed the test database's foreign key constraint; it was moved behind an explicit foreign-key disable in that disposable adversarial database. Production seed constraints remain enabled. Initial lint findings (JSX inside try/catch and a constant test-loop condition) were fixed before final lint. The known Windows wrappers used the documented direct-build/Git Bash fallbacks; the archive was validated before publication.

**24 of 40 complete / 16 remaining.** P5-01 is complete: dashboard/brief/integrity and review-queue readers are scoped, query-backed and source-linked, with the remaining snapshot inventory explicit. Continue P5-02/P5-03 with PM program, exact occurrence/plan readers and their linked-access audit for reopened P4-02. Preserve PM denominator/window rules and remove the nested main landmark during browser acceptance. Other record, equipment/lifecycle, Trends, creation/governance/API migrations plus P5-06 and Passes 6–7 remain open. The full browser release matrix and real infrastructure proof are still later gates.

### Pass 5 review-queue publication checkpoint

Private version **42** published successfully at **2026-09-15 03:54:41 UTC**, from source `98599e9ff7b6d322b021548789dcdca8748d0d07`, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_df3f26f5892c8191a0a3a3da3ec62672`; deployment: `appgdep_6aa8c173734481919bd3f71d49355f21`. Native status confirmed success with environment revision 1. A fresh access read confirmed the owner role, one allowed user and zero groups; private publication preserved that audience. D1/R2 are unchanged. Successful builds, exact source push, Git Bash packaging and archive validation preceded saving/deployment.

**24 of 40 complete / 16 remaining; the goal and automatic continuation remain active.** Resume at PM program and exact occurrence/plan readers in `PASS5_QUERY_INVENTORY.md`. The source inspection confirms `loadPmProgramManagementModel` still loads a snapshot and uses `.length` checks for membership scope; explicitly empty scopes need fail-closed regression in its replacement. Its local status helper also needs reconciliation with the shared PM timing contract. `PlanningWorkspace` contains the nested main at its outer return; fix it for both PM and lifecycle during that acceptance work. `loadPmPlanScheduleSetupModel` lives in `setup-loader.ts`. Do not rebuild the completed overview/brief/queue queries. Facilities overview and the continued browser tab remain at localhost:3000, with normal sizing. Source data and unrelated `dev4.log` are untouched. This publication checkpoint changes documentation only.

### Pass 5 — exact PM records and visit evidence, September 15, 2026

Completed the occurrence detail and store plan editor migration, with scoped parent-first reads, matching-store linked records and bounded visit evidence. Shared fixture/SQLite/PostgreSQL checks and an independent 230-visit adversarial SQLite fixture cover organization/store/region/empty scopes, stable equivalent-timestamp ordering, complete pagination totals and conflicting plan/equipment/work/visit references. Actual authenticated page/loader tests forbid snapshots, retain canceled/late-completion timing and reject records after narrowing the membership store.

UX: short record sections, compact store labels, readable window dates, a phone-stacked visit table, exact work/visit links, clear future-schedule wording and one main landmark in the shared PM/lifecycle workspace. Browser evidence: Store 106 overdue fall-HVAC occurrence shows Past due with no work/visit; completed refrigeration occurrence shows Completed on time with the recorded 07:00–07:55 visit; its visit record retains the cross-channel finish evidence and 55-minute approximate presence. Switching to the Store 104 manager denies the same Store 106 occurrence, then restoring facilities restores access. The original store/status filters survive the back link. Inspected 375px and 1280px layouts and the schedule form; no records were mutated.

Validation: seed passes with 15 stores, five vendors, two internal technicians, 100 requests, 422 work orders, 399 visits, 144 assets and 222 PM occurrences; separate 65-store fixture stays distinct. Final typecheck and lint pass. Full suite: **136 files / 911 tests**, 166.90 seconds. Workflow command: **4 files / 53 tests**, 95.17 seconds (run alongside other checks, not a performance benchmark). Final focused authenticated PM check after small store-label/form-copy refinements: one pass / 20 skipped, 2.50 seconds. `npm run build` and `npm run build:render` pass. Shared native checks include migrated D1-compatible SQLite and embedded PostgreSQL; these do not certify production throughput or external readiness.

Portable Sites helpers were attempted: configuration returns portable/configured false; native build cannot locate its npm wrapper; native packaging cannot start. The standard npm build and documented Git Bash packaging fallback succeed. Validated the deployment archive contains only dist output, the Worker entry, manifest, client assets and bundled migrations. Current private publication remains version 42 until the following publication checkpoint.

**24/40 remain complete, 16 open.** Next: replace PM program/schedule aggregates and exact enrollment/reconciliation/reactive-analysis source queries; repair global ordering and scope/filter preservation. P5-02/P5-03 and P4-02 remain open. Continue other inventory groups and passes 6–7 afterward. Preserve the completed overview/brief/queue/PM-record boundaries. Unrelated dev4.log is untouched.

### Pass 5 — PM record publication checkpoint

Private Site **version 43** succeeded at **2026-09-15 04:19:50 UTC**, with environment revision 1 and the existing DB/FILES bindings. Source commit: `51fdafd64c0478c9b075724038585d3c230b46e1`, pushed to the registered Sites main branch before saving. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_51c0d5b1d37881918e87f80d726bf133`. Deployment: `appgdep_6aa8c75841a081919fef37bbbf92d964`. Production URL: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Access remains custom owner-only: one allowed user, zero groups; no audience or environment changes.

The final archive was rebuilt/repackaged after whitespace cleanup, then validated at 701 tar entries / 678 saved files. Native terminal deployment success is the publication evidence; no production HTTP/browser probe was used. The local development server stays running and the continued browser tab is at companywide PM with maintenance/facilities access and normal viewport sizing. The exact work and visit links both opened matching Store 106 records. Unrelated dev4.log remains untouched.

The goal remains active: **24/40 complete**. Continue the main PM program/schedule migration in the query inventory, then the remaining record/lifecycle/import/operational and acceptance gates. This checkpoint records documentation only after the published implementation.

### Pass 5 — PM schedule and exact comparison sources

The PM schedule now reads scoped native aggregates and 25-row pages, with global due-date ordering, stable timestamp/ID ties, full status totals and exact source links. Due, overdue, missed and unscheduled are separate; completed/waived/canceled states retain their precedence. Closed-window completion keeps the eligible denominator and late completion explicit. Invalid plan/equipment/work links cannot expose another store. Full comparison pages use bounded cost/work/equipment queries and skip the management snapshot entirely. The remaining management panel now intersects selected store/region with membership and verifies invoice references against the same store, but its query migration is still next.

Browser evidence: 222 total occurrences; 14 need attention; 88 upcoming, four due, two overdue, seven missed, 119 completed, one waived and one unscheduled. Overdue opens exactly two records. All page two shows 26–50 of 222. Closed-window completion shows 92/99. Comparison evidence opens 71 USD cost lines totaling $74,525.00, and 41 equipment with 61 other work records; equipment page two retains 26–41 of 41. Store 104 shows only Ridgeview records and six enrolled plans, with store scope retained through the enrollment link. Inspected desktop and 375px source/schedule layouts; fixed a malformed disclosure grid and blue-button text contrast. No browser records were mutated.

The cost source exposed recurring scheduled HVAC service among work without a PM occurrence link. Accordingly, the live comparison now says Other work rather than claiming every record is a repair or occurred after the latest PM window. The rate remains work created in the stated year divided by equipment × 12 months; partial exposure is unadjusted and small groups are identified. Cost dates/currency and exclusions remain explicit, with longer methodology behind a disclosure. Historical snapshot-presenter tests remain compatibility evidence, not the live PM page.

Validation: deterministic seed passes with the unchanged 15-store/five-vendor/two-technician presentation and separate 65-store fixture. Shared fixture, migrated D1-compatible SQLite and embedded PostgreSQL compare states, filters, scopes, cohorts and full totals. A separate 230-occurrence/230-cost adversarial fixture verifies bounded 25-row returns, stable traversal, malformed references and CAD/future exclusion. Actual authenticated schedule/comparison loaders reject snapshot access, and management tests cover selected-store and mismatched invoice facts. The full suite passes **137 files / 915 tests**, **159.52 seconds**. The first run under overlapping builds had one 15-second PostgreSQL price-planning timeout (914 passed); the isolated full rerun passes all tests. Workflow command passes **4 files / 53 tests**, **82.54 seconds**. Typecheck/lint and both builds pass before final packaging; the following publication checkpoint records the exact artifact.

**24/40 complete, 16 open.** Continue with company PM schedules, enrollment/coverage/reconciliation queries, then remaining P5 inventory and passes 6–7. P4-02/P5-02/P5-03 remain open. Native Windows Sites build/package wrappers still require the documented npm/Git Bash fallbacks. Preserve the current private audience and DB/FILES bindings. Normal viewport and companywide facilities PM are restored; the continued browser tab remains available. Unrelated dev4.log is untouched.

### Pass 5 — PM schedule publication checkpoint

Private Site version **44** succeeded at **2026-09-15 05:00:45 UTC**, environment revision 1. Source `22bd47ea431b7c85063bd3f80b5f706104eb0fe0` was pushed to the registered Sites main branch before saving the validated 701-entry archive (678 saved files). Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_cf4390120d0881918ba5671fbc1919e9`. Deployment: `appgdep_6aa8d0ee9e288191a41d69f004036ab2`. URL: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Owner-private access and DB/FILES bindings remain unchanged. Native terminal deployment status is the publication evidence; no production HTTP/browser probe was used.

Final typecheck, lint, Sites build and Render build exited successfully after the last copy and mobile disclosure refinements. Full suite: 137 files / 915 tests; workflow suite: four files / 53 tests. Normal local viewport and companywide PM remain ready for continuation. The independent untracked dev4.log was not modified.

**24/40 complete, 16 open; automatic continuation remains active.** Resume at company PM schedule/enrollment/coverage/reconciliation query migration in PASS5_QUERY_INVENTORY.md. Main schedule, other-work comparison, exact PM records, overview/brief/queue queries are already complete and should stay query-backed. Existing company setup/coverage controls and the accumulating navigation trail still need concise UX acceptance. Do not infer production or full-pass readiness from this publication. This checkpoint changes documentation only.

### Pass 5 — company PM setup and exact coverage

Company program and enrollment calculations now use `listPmSetup` native SQL and a dedicated presenter. Programs, coverage targets and plans have full counts, stable ID ordering and bounded pages. A target is one program plus equipment/store; duplicate store enrollments do not inflate covered counts. Empty equipment rules explicitly mean store-wide work. Unknown nonempty rules remain visible for review and no longer become all-store programs. Exact Covered links select the program/store/equipment (or asset-free store target) before listing plans. Invalid equipment references remain unavailable. Company and plan counts include complete cohorts, not only displayed rows.

All setup metrics now open their exact complete source lists. Main program pages return 25 records, inline plans return 20 and full source pages return 25. The authenticated setup source page forbids snapshots. `loadPmReconciliations` is now the isolated remaining PM snapshot consumer and skips invoice-ineligible roles. It still needs native reconciliation and exact invoice/occurrence/visit sources; do not claim the whole PM page or P4-02/P5-02/P5-03 complete.

UX: shorter Company schedules wording, compact coverage controls, readable 15px setup text, 44px phone links, no repeated identical plan/program names and unbroken result counts. Recently viewed keeps per-tab navigation history accessible without displaying six accumulated breadcrumbs above every page. Browser checks: five programs, 88 targets, 88 active plans and zero gaps; all target sources show 1–25 then 26–50 of 88 across four pages. The first rooftop unit opens exactly one Store 101 fall-HVAC plan with that scope and program visible. The zero-gap source has a clear empty state and an All targets control. Desktop 1280px and phone 375px layouts were inspected; normal viewport and companywide facilities PM restored. No source records were changed through the browser.

Validation: deterministic seed passes, with 15 stores/five vendors/two technicians and the separate 65-store fixture unchanged. Shared fixture, migrated SQLite and embedded PostgreSQL compare every query kind, complete totals, filters, scope intersections and empty/foreign scopes. An independent SQLite fixture adds 230 programs, dense enrollments, an alias rule, an unknown rule and duplicate store enrollment; complete traversal stays within 25 returned rows. Its final cross-store equipment mutation verifies unavailable display and a real gap. The first malformed mutation collided with an existing program/equipment uniqueness constraint; choosing an unenrolled foreign-store equipment record corrected the adversarial setup without weakening production constraints.

Full suite passes **138 files / 916 tests**, **154.62 seconds**. Workflow command passes **4 files / 53 tests**, **83.48 seconds**. Final authenticated/navigation focused checks pass 31 tests; the amended dense scope check passes separately in 2.53 seconds. The initial PostgreSQL test selection skipped the seed test and was not valid isolation; running the actual migration/seed/query test passes in 43.60 seconds, and both full suites pass. Final typecheck/lint and build results plus exact publication follow below. These checks establish query behavior, not external production throughput.

**24/40 complete, 16 remain.** Next is the PM invoice reconciliation reader and source pages, then remaining record/lifecycle/Trends/import and operational/release gates. Keep completed PM and overview/brief/review boundaries. Preserve private access, D1/R2 and unrelated dev4.log. Automatic continuation remains active.

### Pass 5 — PM setup publication checkpoint

Private Site version **45** succeeded at **2026-09-15 05:23:48 UTC**, environment revision 1, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Exact pushed source: `f83b9b190d7b10ba6c896d7e25beccc656114a54`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_cb5bd457af3c81918b5e915eed9e756d`. Deployment: `appgdep_6aa8d657aaf881919df17553b098bbb0`. Owner-private access remains one allowed user/zero groups, with DB/FILES unchanged. The archive was validated at 706 entries / 683 saved files before saving. Native terminal success is the publication evidence; no production HTTP/browser probe was used.

Final typecheck, lint, Sites and Render builds passed. The Sites build reported duplicate emitted CSS filenames and completed successfully; retain this observation for the final packaged-browser acceptance. Native Windows build/package wrappers failed as previously documented, and the ordinary npm build / unchanged Git Bash packaging helper succeeded. The full 916-test and 53-workflow-test runs passed; late copy/spacing changes were checked in the browser and final type/build checks. The final adversarial SQLite amendment passed separately after preserving its unique enrollment constraint.

**24/40 complete, 16 remain; continue automatically.** The next concrete task is `loadPmReconciliations` and its exact source pages, documented in PASS5_QUERY_INVENTORY.md. It is the remaining full-snapshot dependency of the PM overview. Keep current setup/schedule/comparison/exact-record queries intact. The local facilities PM tab is retained, normal viewport restored, and dev4.log untouched. This checkpoint changes documentation only after publication.

### Pass 5 — complete PM page and invoice review sources

Removed the last PM snapshot helper and added native `listPmReview` queries with a separate feature presenter. The complete PM page now rejects snapshot access in authenticated tests. The five-row review preview preserves the total; reviews, invoices, PM windows and visits have complete 25-row source pages, stable date/ID ordering and totals beyond the last page. Store, region and membership intersect before parent and linked records. Review sources require invoice access even when the selected source is a visit or occurrence.

The review uses current linked invoice allocations and all-channel visit evidence rather than trusting stored summary amounts or treating the last invoice as the whole review. Full invoice visibility requires every allocation to resolve within the same authorized store. Program allocations determine amounts. Missing and contradictory references stay visible as unavailable counts; malformed facts cannot expose a record. Mixed currencies remain separate. Current cross-channel evidence can reduce missing windows without changing the original recorded review. The interface presents missing evidence as a review fact, never proof of nonperformance or a payment decision.

UX and browser: replaced the long amber reconciliation cards with one compact table, short labels and exact sources. Desktop 1280px and phone 375px layouts were inspected; mobile rows retain an explicit Without visit evidence label. The local seed has four invoices/$1,700, two visits, four PM windows, and two windows without visits/$850 linked invoice amount. Full and filtered invoice lists agree, visit sources show two records, missing windows open the exact occurrence and return to the same source filter. A Store 104 manager is denied the already-open Store 107 review; facilities access is restored. No source records were changed through the browser. These local values are not asserted as the current values of the persisted hosted tenant.

Validation: deterministic seed passes (15 stores, five vendors, two technicians; separate 65-store fixture). Typecheck and lint pass. Shared fixture/migrated SQLite/embedded PostgreSQL contracts pass. Independent SQLite density tests traverse 231 reviews, 35 invoices and 33 visits without duplicates or any result above 25 rows. Adversarial checks cover duplicate/non-string/malformed facts, mixed USD/CAD, unknown and cross-store references, scope denial and a second work link on a recorded visit. Initial test failures were corrected fixture expectations (two seeded visits), a missing synthetic visit-work link and an invalid test actor enum; production constraints were retained.

Full test run: **139 files / 919 tests passed**, **162.84 seconds**. Workflow command: **4 files / 53 tests passed**, **83.02 seconds**. Sites and Render builds pass; existing duplicate CSS filename warnings in Sites remain for final packaged-browser acceptance. The native Windows build/package wrappers still fail before the build/package process; standard npm build and unchanged Git Bash packaging succeeded. The archive contains only dist output, with 708 entries, Worker entry, DB/FILES manifest, client assets and bundled D1 migrations. The exact linked invoice detail also opens successfully with its $425 amount and matching work order. Publication evidence follows below.

**24/40 complete, 16 remain.** The complete PM overview, setup, review and comparison paths are query-backed. P4-02/P5-02/P5-03 stay open for the wider record/work-action, equipment/lifecycle, Trends and other readers in PASS5_QUERY_INVENTORY.md. Continue automatically with those readers and the remaining import/operations/release gates. Keep source IDs, private access, D1/R2 and unrelated dev4.log intact.

### Pass 5 — PM invoice review publication checkpoint

Private Site version **46** succeeded at **2026-09-15 05:53:36 UTC**, environment revision 1, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Exact pushed source: `ae288e25590fa9d28deea2e634ef4693c375dc91`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_673073cdaf7481918d3520afd416a807`. Deployment: `appgdep_6aa8dd51cffc8191a95de4ae832189aa`. The verified owner-private audience remains one owner/zero groups; DB/FILES remain unchanged. Archive validation found 708 entries; the saved version reports 685 files. Native terminal deployment success is the publication evidence. No production HTTP/browser probe was performed.

All required checks passed: seed, typecheck, lint, 919 tests, 53 workflow tests and Sites build; Render build also passed. Final phone caption/anchor spacing was visually checked and included in both builds. CSS filename warnings remain documented for final packaged acceptance. Source data was not mutated through the browser, facilities access is restored, and the normal-viewport local PM tab is retained.

**24/40 complete, 16 remain; automatic continuation stays active.** Next concrete task: the invoice/warranty detail reader and its scope/line/evidence/history queries. The exact invoice detail reached from this review still uses `loadWarrantyFinanceWorkspace` and a compatibility fixture; its accounting-source guard also needs explicit-empty-scope review. Continue through the wider record, lifecycle, Trends, import, operations and release checklist afterward. This checkpoint changes documentation only after publication; dev4.log remains untouched.

### Pass 5 — bounded invoice detail and simpler review

Replaced the invoice detail's tenant snapshot and retired its old fixture renderer. The complete page uses `loadInvoiceRecord` and native `readInvoiceRecord` queries. Five focused sections replace the long combined page: invoice items, work matches, service evidence, review flags and review history. Each has full counts and stable 25-row pages. Exact item/match links survive pagination; reporting links now include the exact allocation selector. Linked amounts open their supported matches, unmatched amounts open unmatched item balances, and open-flag counts open only open flags. Source scope, invoice, amount basis and currency stay visible.

Parent visibility checks every allocation against its work and allowed store before returning the invoice. Empty scopes deny access; an unallocated invoice is available only to a genuinely companywide caller. Vendor agreement and visit references are validated, and unavailable evidence remains explicit. Invoice events, adjustments, confirmed benefit entries and eligible accounting metadata are paged, with currencies kept separate. Accounting access distinguishes undefined scope from an explicit empty list. Authenticated tests reject snapshots for every section and deny role/store conflicts.

Review controls mirror companywide role/write access and prevent the submitter reviewing their own invoice. The domain command now independently rejects store-only and ungranted reviewers. Existing real-account read-only request checks remain at the API boundary; the historical preview finance-read grant is not a new production write permission. The form shows a deduction amount only for a deduction, requires a reason, explains how resolving the last flag affects the approved amount, and returns to the flags with a saved confirmation. No payment is executed.

Browser checks at 1280px and 375px: readable invoice totals and source rows; clear blue links; five sections; exact $1,650 labor match to Store 104/work/visit; filtered open flag; conditional deduction field; native required-reason prompt; and Store manager denial. In the local fictional fixture only, the Invoice reviewer waived the remaining authorization flag with reason `Local fictional-data validation of the revised invoice review form.` The saved page displayed the reason and a new Parker Shaw history entry. Invoice total remained $11,575, approved amount became $11,575, and recorded paid amount remained $0. The native role was restored to facilities and viewport reset. This local mutation was not performed on the hosted tenant.

Validation: deterministic seed remains 15 stores/five vendors/two technicians, with the separate 65-store fixture. Typecheck/lint pass. The full suite passes **140 files / 922 tests**, **168.30 seconds**; workflow command passes **4 files / 53 tests**, **95.50 seconds**. Final focused invoice/UI/identity/command checks pass **42 tests**, **15.58 seconds**, after the last display changes. Shared fixture/migrated SQLite/embedded PostgreSQL queries cover every section/filter, scope and complete totals. Separate SQLite traversal covers 231 items/matches, 230 flags/history entries, timestamp ties, mixed-currency adjustments, removed confirmation, exact match and conflicting visit/store references, never returning more than 25 rows. The first PostgreSQL UNION failed on mixed timestamp/text columns; explicit timestamp casts corrected it. A proposed stronger direct-command write check conflicted with the historical preview finance-read grant; the final change enforces company scope while preserving the existing authenticated API write boundary.

Sites and Render builds pass. Native Windows workflow wrappers retain their known startup failure; standard npm build and the unchanged Git Bash packaging helper succeeded. Existing duplicate CSS filename warnings remain recorded for final packaged-browser acceptance. Exact publication evidence follows below.

**24/40 complete, 16 remain.** Next is the invoice queue/intake, then warranty and the other record/lifecycle/Trends readers. Align queue visibility and financial source cohorts with this exact reader. Include a cross-feature check of PM review allocation amounts versus confirmed linked-reporting amounts when a confirmation is missing or item totals do not reconcile; those are distinct inputs in the current readers and must not be silently described as one basis. Keep P4-02/P5-02/P5-03 open and preserve the operational/release gates, private access, D1/R2 and dev4.log. Continue automatically.

### Pass 5 — invoice detail publication checkpoint

Private Site version **47** succeeded at **2026-09-15 06:38:35 UTC**, environment revision 1, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Exact pushed source: `a6bdfc84dab1b4dc846e964c88f54c996c80b8ab`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_036f1592aac481918beba5d5e01f9b98`. Deployment: `appgdep_6aa8e7e009ac8191aa1b5a42273a51d1`. Owner-private access and DB/FILES are unchanged. The validated archive contains 717 entries, 155 client files and 51 bundled D1 migrations; the saved version reports 694 files. Native terminal success is the publication evidence; no production HTTP/browser probe was performed.

Required seed, typecheck, lint, 922 tests, 53 workflow tests and Sites build passed; Render build also passed. Desktop/phone invoice browsing and a local fictional review mutation were checked as recorded above. The local facilities role and normal viewport are restored. Existing duplicate CSS filename warnings remain for packaged acceptance. This checkpoint changes documentation only after publication; unrelated dev4.log is untouched.

**24/40 complete, 16 remain; continue automatically.** Next concrete task: replace the invoice queue/intake snapshot path, align its visibility with exact invoice parents, and make its measures open exact source cohorts with currencies preserved. The current queue's exposure sum is forced to USD and opens a generic review filter; retain this as an explicit fix in that batch. Verify PM review raw allocations versus confirmed linked amounts as part of the same financial-source consistency work. Warranty, other records/actions, lifecycle, Trends, import and operational/release gates remain afterward. Do not treat this publication as completion of P4-02/P5-02/P5-03 or of the whole platform.

### Pass 5 — invoice queue interruption checkpoint

Uncommitted local implementation adds `listInvoiceQueue` for all invoices, invoices with open flags, exact open flags and exact identified-exposure events. Organization/store/work parent validation matches the invoice detail boundary; unallocated invoices require companywide scope. Search and 25-row pages run in the repository with full counts, stable ordering and currency-specific exposure totals. The compact queue uses exact metric destinations and an exact invoice flag selector. The old invoice queue renderer is removed. Intake and filtered Trends invoice evidence still use snapshots and remain unfinished; PM raw-allocation versus confirmed-linked consistency is also still pending.

Validation so far: deterministic seed and typecheck passed. Focused UI/identity checks passed (30 tests), followed by both invoice SQLite query suites (2 tests, 14.36 seconds). The density fixture initially reused value-event deduplication keys; unique synthetic keys corrected that test setup while preserving the production constraint. It now proves 230 invoice/flag records, separate 115-event USD/CAD cohorts, bounded traversal and contradictory-parent denial. Lint found the obsolete date helper in the retired queue module; that unused helper/import was removed, but lint has not yet been rerun.

The full suite and workflow suite were started concurrently and **both failed their PostgreSQL migration/seed/query test at the 120-second timeout**. Full result: 140 files passed/1 failed, 923 tests passed/1 failed, 364.12 seconds. Workflow result: 3 files passed/1 failed, 52 tests passed/1 failed, 354.10 seconds. Resource contention is a hypothesis, not a verified cause. Both processes have exited. Resume by diagnosing/rerunning the PostgreSQL check independently, without increasing the timeout to hide a query regression, then rerun required checks sequentially. The native Windows Sites build helper hit its known missing npm helper startup failure; the ordinary build fallback has not yet run for this change. There is no validated new archive or publication.

Browser inspection reached the new desktop queue (98 invoices, 11 open flags after the prior local fictional waiver, four USD exposure entries totaling $21,850), opened the exact 11-flag list, and navigated to the selected warranty flag. Final destination verification, search/pagination, mobile and role checks remain. A 1280px viewport override may still be active and must be reset after completing browser validation. No new source records were mutated in this queue work. Keep unrelated dev4.log untouched. The user clarified that they reset usage themselves; no assistant reset credit was consumed. **24/40 complete; goal and automatic continuation remain active.**

### Pass 5 — invoice queue validation resumed

The isolated PostgreSQL check reproduced the timeout (185.71 seconds), ruling out test concurrency as the sole explanation. The invoice queue used a correlated nested allocation/work scope lookup. Replacing it with a grouped allocation-scope relation retained the same parent guard and passed the unchanged PostgreSQL migration/seed/query regression in **43.90 seconds**; no timeout was increased. The changed SQLite density query passed in **3.53 seconds**. This embedded PGlite test never connects to Render; a user-reported possible Render database suspension is separate and has not been verified.

Typecheck and lint passed. The next full suite passed 922 tests and failed two held-work assertions because its vendor gateway used the real date after a fictional hold deadline. Those two tests now set the Date clock to their fixture's as-of timestamp, retaining the existing explicit deadline/claim checks and all production rules. The focused held-work suite passed **14 tests**, **21.61 seconds**. Final checks are running sequentially to avoid another overlapping full/workflow run.

Browser verification now confirms the exact warranty flag (one source record), all 11 open flags, four USD exposure entries/$21,850, CAD zero-state without currency mixing, GREENLOT search (42 invoices) and page two (26–42 of 42) with the search preserved. Desktop and 375px phone rows were visually inspected; action/link contrast was corrected. The facilities role remains active, viewport override is reset and the unfiltered invoice queue is retained for handoff. No new source data was mutated.

**Next financial consistency repair has a verified counterexample:** in a disposable fixture, removing the confirmation from `invoice-summit-107-pm-2026-q3` reduces the exact invoice linked amount from 42,500 minor units to zero, but its PM review invoice source still reports 42,500 and its review total remains 170,000. The PM reader currently sums recorded allocations while labeling them linked. Repair this before final acceptance of financial drill-through; retain the unconfirmed invoice/evidence visibly rather than dropping the review. This is not a Render problem. The wider gates remain unproven; do not use historical P2 completion notes as proof of this newly discovered cross-feature condition.

Final queue validation: **141 files / 924 tests passed, 165.23 seconds**; workflow command **4 files / 53 tests passed, 86.03 seconds**. Seed, typecheck, lint, Sites build and Render build passed. The final held-work changes affect test Date setup only; no production deadline or authorization was relaxed. Native Windows Sites wrappers retain their known startup failure; the ordinary npm build and unchanged Git Bash packaging helper succeeded. Validated archive: 720 entries, Worker entry, DB/FILES manifest, 51 D1 migrations; saved version reports 697 files. Existing duplicate CSS filename warnings remain a final packaged-acceptance observation. Publication evidence follows.

### Pass 5 — invoice queue publication checkpoint

Private version **48** succeeded **2026-09-15 16:29:03 UTC**, environment revision 1, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Exact pushed source: `9e04922cba7aca81dd1cdfb69cb251532850a8cf`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_703f800e185c8191b9df8dfc52cfdc7e`. Deployment: `appgdep_6aa9723c9d088191922f7f2b4b733b42`. Verified owner-only access remains one owner and zero groups; DB/FILES are unchanged. Terminal deployment success is the publication evidence; no production browser/HTTP probe was used.

**Continue automatically; 24/40 checklist items remain accepted and 16 remain open.** This publication completes the ordinary invoice queue migration, not the broader record/financial-source gate. Next: fix the verified PM allocation-basis counterexample, then finish the invoice intake and filtered evidence journey as one batch. Keep tests focused during implementation and run required full checks once for the complete batch, sequentially. Preserve the normal-viewport local facilities invoice tab, the prior fictional waiver and unrelated dev4.log. This checkpoint changes documentation only after publication; there are no remaining live validation processes from this batch.


### Pass 5 — invoice intake and linked-amount repair

The intake route now uses authenticated, bounded work/vendor/agreement queries rather than a tenant snapshot. Search and 25-row pages preserve selected work and vendor. One required item is shown initially; users can add up to 100 items. Agreements and files stay optional. Save errors remain in the form with entered values preserved and duplicate submission is disabled while saving. Companywide role/write access is checked in the loader; direct intake commands now reject store-only or absent grants. Duplicate-number, prior-work invoice and active warranty-hold checks use bounded native queries instead of scanning every invoice and warranty case. The obsolete receive workspace is removed.

The verified PM counterexample is repaired: unsupported or unconfirmed allocations remain visible in the review cohort but contribute zero to linked amounts. Shared canonical facts now align PM, exact invoice detail and fixture reporting for confirmation, currency, reconciliation, over-allocation, voids and non-maintenance bills. Malformed accounting JSON is tolerated; boolean false is distinguished from the text string false in the exact SQL reader. This does not complete the separate filtered Trends evidence migration or the wider financial acceptance gate.

Focused PM/UI checks passed (7 tests); intake command and embedded PostgreSQL checks passed (23 tests, 82.40 seconds). The authenticated boundary test verifies intake without snapshots, invalid selections, restricted scope and read-only denial. A five-item API test verifies all five items and a $51.25 total. Its initial failure expected HTTP 400 for invalid count; the established API correctly returns 422, and the assertion was corrected. Seed remains 15 stores/five vendors/two technicians plus the separate 65-store fixture. Final full validation is in progress; no completion or publication is claimed by this entry.

Browser: work search selected the exact CPS-2026-0104; five vendors and the vendor-specific optional agreement were verified. Duplicate SUM-104-2607 showed an inline error while preserving all fields. Keyboard add/remove controls worked. The local fictional invoice LOCAL-INTAKE-0915 was saved as invoice-21e80982-87a7-453b-af2d-7cca6820ac40 with two items ($10.25 and $5.75), total/linked amount $16.00, unmatched $0 and one review flag. No hosted source data was mutated. Desktop 1280px and phone 375px were inspected; the primary button color specificity was corrected. The facilities role is unchanged.

**24/40 accepted, 16 open.** P4-02/P5-02/P5-03 remain open. Next: finish filtered invoice evidence from Trends, then the remaining warranty, record/action, lifecycle, import and operational/release gates. Preserve IDs, private access, D1/R2 and unrelated dev4.log. Continue automatically.


Final intake validation: seed, typecheck and lint pass; **141 files / 926 tests passed in 179.56 seconds**; workflow checks **4 files / 53 tests passed in 86.60 seconds**. Sites and Render builds pass. The existing duplicate emitted CSS warnings remain a final packaged-acceptance observation. The final blue save button was visually verified, the selected agreement returned correctly to the form, and the facilities invoice queue was restored with the viewport override reset. The archive uses the already-established Git Bash packaging fallback for the native Windows helper startup failure. Publication evidence follows after terminal success. No checklist acceptance item was closed by this partial migration.


### Pass 5 — invoice intake publication checkpoint

Private version **49** succeeded **2026-09-15 16:57:05 UTC**, environment revision 1, at https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Exact pushed source: `86b0017172c127e2b7a173d3e3192015a6e583ee`. Version: `appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_a998f1f46df48191b35259f2ebab1801`. Deployment: `appgdep_6aa978cb7a1c81918b0db4233bd59136`. Private audience and DB/FILES are preserved. Validated archive: 730 entries, 51 migrations; saved version reports 707 files. Terminal success is publication evidence; no production browser/HTTP probe was performed.

Seed, typecheck, lint, 926 tests, 53 workflow checks and both builds passed. The local invoice queue is retained in the normal viewport with the facilities role; LOCAL-INTAKE-0915 is the only new browser-created fictional record in this batch. Unrelated dev4.log remains untracked and untouched.

**24/40 accepted, 16 open.** Next is the filtered invoice evidence route from Trends, then the outstanding records/actions, lifecycle, imports and operational/release gates. User authorization to continue persists. A fresh get_goal read reports `usageLimited`, not active; no tool has resumed or changed that usage control. Do not claim automatic goal continuation has resumed solely because the user previously reset account usage. This checkpoint records publication only and does not close Pass 5.


### Consolidated remaining-work batch — in progress

The user explicitly requested one implementation batch followed by final validation to reduce usage. Current changes replace the filtered invoice evidence snapshot with a native paged query and full totals, preserve its source filters and use exact allocation links when the whole invoice is visible. The focused SQLite/spending regression passed 19 tests; full validation remains deferred. Work review now invokes its existing repository reader directly. Held-work and vendor-response actions check the exact work/store scope first; vendor response selection returns the latest unhandled actionable response. Job health no longer loads source records. Vendor qualification uses that vendor's specialties, and onboarding resolves only selected coverage/specialty values. These changes are not yet acceptance evidence for a whole item.

Continue this batch before testing/publishing: remaining record/action, creation, warranty, lifecycle, Trends, governance and import readers; operational code gaps and the final browser/validation/documentation matrix. External infrastructure gates remain explicitly unproven. Keep 24/40 accepted and preserve dev4.log. The saved goal's usageLimited state does not cancel the user's explicit current-turn and heartbeat authorization to work.


Current batch additions: job-health summaries now require companywide access; scoped facilities cannot infer companywide job/outbox counts. Native vendor onboarding selection is bounded to submitted keys/coverage IDs, with exact organization predicates. Added queued regression coverage for work action scope denial, no-snapshot loaders, selected vendor lookups and actionable response parity; these checks have not yet run. Fixed optional-region import validation, added 25-row preview pages and recoverable network errors, and separated recorded work-cost currencies. Production import application remains unimplemented and unaccepted; preview changes do not close P5-06. No full suite or publication has been run for this batch.


### Consolidated batch validation checkpoint — September 15, 2026

Added native paged warranty queue/source counts, query-backed request creation with a scoped store picker, and small configuration reads for store/vendor onboarding. Removed duplicate nested main landmarks in five working surfaces. The request form now defaults to Routine and removes repeated instructional boxes. Store selection survives searches; larger networks receive 25-option pages and forward/back controls.

Closed explicit-empty-scope leaks in compatibility record, Trends, approved-work, verification, workflow-task, service-run, store-sweep, setup and value-ledger readers. Companywide import and approval-policy setup reject restricted callers before reading source data. These repairs do not complete the remaining snapshot migrations or restore broad P4-02 acceptance by themselves.

Validation in progress: deterministic seed and typecheck passed. Targeted query/identity/navigation/recording checks passed 52 tests. Full suite reached 930 passing tests with one obsolete text-only assertion requiring a removed instruction box; that assertion was removed while retaining the checks that intake has no required impact fields, and the affected five-test file passed. Initial lint found three unused presenter type imports after extraction; removed them. Workflow, final lint and builds still pending at this checkpoint.

Browser evidence: at 375 px, warranty source drill-through shows one $8,900 flagged amount and its exact case; repeated diagnosis labels removed. Request creation preserves Store 104, defaults Routine, blocks an empty problem through native validation, and saves fictional request `request-461f9f1c-743f-40fb-b756-a5ec72ab90af` (REQ-72AB90AF), reporter Local QA, problem “Local validation: cooler door does not close fully.” This mutation is local only. Invoice evidence retains Store 104, Sep 1 2025–Aug 25 2026 and USD, showing 11 confirmed allocations totaling $24,286.40. Full cross-platform matrix remains pending.

Acceptance remains 24/40. Next local migrations remain Trends aggregates/exports; record/action/program/lifecycle readers; work-order creation; approval governance/vendor performance; warranty detail; import preview/apply and remaining APIs. Infrastructure-dependent delivery, private-storage, real PostgreSQL, recovery and load drills remain unproven. Version 49 remains the published baseline; this batch has not been published.


### Demo-first sequencing — September 15, 2026

The user clarified that desktop demo readiness is the immediate objective; vendor check-in/check-out is the only required mobile journey for now. Stop additional operator-phone polishing. Preserve the existing checklist and all open production/architecture gates, but prioritize the desktop demonstration walkthrough and its blocking defects ahead of further snapshot migrations. This explicitly changes execution order without claiming broader production readiness.

### Desktop demo batch — final verification

User priority remains desktop demo readiness, with mobile limited to vendor check-in/out. Vendor onboarding now uses named specialty and coverage checkboxes, with a short optional custom-specialty field. Repeated request and vendor instructions were removed. Multiple selections and missing coverage have API regression coverage.

The vendor mobile journey was completed locally at 390 px on CPS-2026-0116: QR check-in and secure-link checkout, location declined, return visit required. A browser-discovered receipt inconsistency was repaired: receipts and retries now use the persisted follow-up owner/action rather than the requested vendor. Desktop work and lifecycle now show Jordan Lee, “Propose the return service date,” due Sep 16 at 1:55 PM. The corrected receipt is covered by integration tests; it was not resubmitted in the browser. Local visit visit-95ffa560-15ca-4583-ae76-46fbe09e093a is fictional QA data, not a source seed change.

Desktop walkthrough verified global search to Store 104, prefilled work creation with deferred classification, internal/outside routing choices, named vendor onboarding, PM Completed 9 opening exactly nine scoped records, and Planning register 10 opening ten records and the beer-cave comparison. That comparison preserves repair price, planning estimate, recorded-cost basis, source links and the saved next action. Invoice evidence, warranty source links and successful request intake were checked earlier in this batch. Import preview was inspected but no file was uploaded. Operator-wide mobile polish is deferred by user direction. Local overview restored and temporary viewport reset.

Final full suite: 141 files / 933 tests passed in 178.14 seconds. Typecheck and lint pass. Final workflow checks and both builds are running. No infrastructure or broad migration acceptance is implied: 24/40 accepted, 16 open. This demo-first batch does not close Pass 5 or the complete production checklist.

Final validation completed: seed, typecheck, lint, 933 tests, 54 workflow tests (90.20 seconds), Sites build and Render build all pass. Existing duplicate CSS emission warnings remain; both builds exit successfully. Publishing this combined desktop-demo batch privately; no broad production checklist item is closed.

### Desktop demo publication checkpoint

Private version 50 succeeded September 15, 2026 at 18:08:38 UTC, environment revision 1: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Pushed source 3f28c9b48ec6a8643d5050bd88d22c7af2f79753. Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_aaeeaaf53e2c8191ac6583b50ffe9790. Deployment appgdep_6aa98997aeb881918a7362058be12dd0. Validated archive contains the server entry, client assets, DB/FILES manifest and 51 migrations; saved version reports 716 files. Private audience preserved. No production browser/HTTP probe performed. All validation evidence above applies to this batch. Unrelated dev4.log remains untouched.

24/40 accepted, 16 open. Immediate priority remains desktop demo usability and vendor mobile check-in/out only. General operator mobile polish is deferred. Remaining production scope, snapshot migrations, import application and infrastructure gates stay open; do not restart those ahead of demo-blocking feedback or describe the whole checklist as complete.

### Acceptance evidence reconciliation — September 15, 2026

P6-07 is accepted for the exact private version 50: validated archive, pushed source, owner-private deployment enforcement and terminal success are recorded above. P7-02 is accepted for the same batch: all required commands passed, with full and workflow results distinguished. These two gates do not require falsely completing the remaining architecture or infrastructure work. Current count is 26/40 accepted, 14 open; older counts in this chronological log remain historical.

Corrected the stale top checkpoint (version 49/uncommitted), current demo seed counts, obsolete package names and walkthrough labels. README and the dated implementation inventory now direct readers to this checklist; DEMO.md prioritizes desktop and the single vendor mobile journey, distinguishes preview imports from apply, and avoids claiming actual outbound delivery. P7-04 remains partial until the complete branding/status audit is done. This checkpoint changes documentation only: no application tests, rebuild or duplicate deployment is needed. Next: remaining desktop demo issues and P7 documentation/browser automation, then the still-open P4/P5/production gates in recorded order. Keep external proof explicitly unclaimed and preserve dev4.log.

### Hosted access regression — September 15, 2026

User reported “Workspace setup needed / Ask your administrator to finish setting up sign-in” on the published demo. Sites runtime revision 1 still contains OPS_ACCESS_MODE=preview; recent error-only worker logs are empty. The access policy reads process.env while the custom Worker receives runtime bindings as its env argument and previously did not bridge the access settings. Added an explicit bridge for OPS_ACCESS_MODE and OPS_IDENTITY_PROVIDER before request dispatch. Missing bindings clear stale values and retain production fail-closed behavior; no Site audience or identity fallback is changed.

Regression checks cover hosted production plus explicit preview, missing settings after a previous preview value, and configured authenticated mode. Full required validation is running before publication. Earlier local browser checks and terminal deployment success did not prove hosted application access; the prior unqualified demo-ready statement was too strong. Treat hosted access as unresolved until the correction is deployed and the user-visible result confirmed.

Hosted access repair published privately as version 51 at 18:28:21 UTC, September 15, environment revision 1. Exact source 8cb0402b3581a4fc817080165d851049d78d7b5c is pushed to GitHub and Sites. Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_9c6a0686b2708191bca34be3bd661331; deployment appgdep_6aa98e367ac48191bde03329eefa9f58. Seed, typecheck, lint, 142 files/936 tests, 54 workflow checks and Sites build pass. Validated archive retains DB/FILES and 51 migrations. Existing CSS emission warnings remain. No production browser access confirmation is claimed; user refresh is the outstanding visible-access confirmation. Checklist remains 26/40 accepted with 14 open. Unrelated dev4.log untouched.

Render clarification: the remaining setup message is on traceops-convenience-demo.onrender.com/access?reason=configuration, not Sites. The prior Worker repair does not affect Render. Its explicit runtime setting must be OPS_ACCESS_MODE=preview for the user's authorized fictional no-login demo. Corrected .env.render.example and the Render runbook; Git changes do not change the actual Render service environment. Actual Render setting/redeployment remains pending until dashboard access or user action; no claim of restored Render access.

### Demo status and branding audit continuation — September 15, 2026

Working tree was clean except unrelated dev4.log. Updated the top checkpoint to the actual version 51 and separated the unresolved Render runtime setting from Sites publication. Read-only branding audit found visible product identity centralized in lib/product/presentation.ts and consumed by root/operator/public metadata, shell, public footer, access screen and job-worker labels. Email copy uses service-record language rather than a second hardcoded product name. Remaining branded string occurrences in active TypeScript are explicitly named legacy cookie/token/storage/header/lock compatibility identifiers; do not rename those without compatibility proof. Favicon is a configured asset path; its artwork is not generated from the monogram token. app/traceops.css remains a legacy-named file with no import found in app/components. P7-04 stays open pending the complete historical-file/branding contract review; no acceptance item was closed by a string scan.

Render demo access remains the immediate external dependency already reported to the user. No credentials, access settings or hosted records were changed, and no duplicate deployment or full test run was performed for this documentation-only checkpoint. Continue desktop demo acceptance and remaining checklist work; no broad operator mobile pass is authorized by the latest user priority.

### User-directed UX review — September 15, 2026

User confirmed Render demo access now works. New immediate product priorities supersede scalability-only work: simplify work-order active/history navigation; let equipment be browsed as all records, by equipment type (including dispensers/gas pumps), and by store; expose a companywide vendor performance overview; replace the weak annualized-spend presentation with useful cost-driver and budget decision support. Preserve original checklist IDs; these are newly discovered UX acceptance gaps, not completed work.

Source review findings: work-order lists mix status choices and held-work portfolio filters; Equipment defaults to attention and offers all/recent/attention plus category filters but no first-class type/store browse modes. VendorPerformanceListComplete still renders portfolio metrics and vendor measures; Accountability selects the contact/routing-only directory. Trends planning computes a continuation-of-history scenario from twelve months; the annualized value is not a bottom-up budget. These findings come from source review, not a fresh browser walkthrough.

Proposed grouped UX acceptance: (A) obvious Active / History / All work navigation, separate status from dates, retain search/store/vendor and show removable filters; (B) Equipment: All equipment / By type / By store, with exact type and store drill-through and optional Needs attention; (C) Vendors: Overview / Directory, with scoped comparable metrics and source links; (D) Spending: separate recorded cost drivers from next-year planning, show PM commitments, authorized open work, human-approved replacements, adjustable reactive allowance and visible unknowns without double counting. User's preferred planning decision has been asked; no financial model or application change has been made in this review.

Research completed for the new UX request. See docs/DEMO_UX_PLANNING_RECOMMENDATION_2026-09-15.md for primary sources, recommendation and open UX-R1 through UX-R4 acceptance. User asked us to choose the planning direction after research. Recommendation: separate Spending from Plan; include scheduled PM, priced open work, explicit reactive allowance and human-selected replacements, with visible unknowns and no double counting. Work Active/History/All, equipment All/By type/By store, and vendor Overview/Directory form the grouped UX work. No application changes or new acceptance completion claimed in this research step. Render access was confirmed working by the user.

User stopped the scheduled follow-up and clarified that UX improvements must consolidate existing features, not duplicate them. Heartbeat finish-platform-improvement-passes paused at user request. No application changes were made by the interrupted continuation. Before implementation, map each proposed change to the existing work list, equipment program, vendor metrics, Trends and lifecycle views; reuse those surfaces. Existing comparison, vendor performance and replacement planning are not new features to rebuild. New editable budget planning remains a proposal, not an approved duplicate dashboard.

### Consolidated c-store UX batch — September 15, 2026

Implemented on existing surfaces: Active / History / All work; the bare work route defaults Active while explicit source links retain their filters. History contains closed/cancelled records and omits irrelevant bulk follow-up and held-work controls. Saved views are collapsed and portfolio cards no longer push the work list down the page. Equipment defaults to all records and offers By type / By store, exact type/store links, gas-pump search wording and optional attention/recent filters. Equipment analysis is secondary and collapsed. Vendor Overview / Directory & contacts are explicit in both preview editions. Planning uses current open work and entered repair estimates, missing-price counts, paginated source records, and existing PM/lifecycle routes; historical extrapolation is collapsed.

Browser evidence (local fictional tenant): 34 active + 388 history = 422 work orders; History and All work switch correctly. Equipment groups show 10 types / 144 assets; dispensers show 60 across 15 stores and four at Store 104 with the type retained. Vendor overview shows five approved vendors and source-linked measures; Directory displays dispatch contact, coverage and open work. Planning shows 32 open jobs, four priced for $36,650 and 28 not priced in USD; page 2 of 4 works. Desktop screenshots were inspected and a weak selected-tab style was repaired in the actual enterprise stylesheet. Temporary viewport reset. No new browser mutations in this batch.

Validation: deterministic seed and lint passed. Initial full suite: 937/940 passed; two obsolete expectations asserted the old equipment landing and hidden vendor metrics, and one invoice query test exceeded its 30-second deadline. Updated those expectations to the requested behavior without removing their checks. Workflow suite passed 54/54, including the revised equipment contract. A serial recheck passed 24 tests across new navigation/planning, vendor edition, invoice query and operator query files; the previously timed-out invoice test passed without changing its timeout. Added planning scope, empty-scope and pagination coverage. An initial final typecheck found a test referencing vendorId directly on work rather than its assignment; corrected that assertion. Final typecheck and both builds pending at this checkpoint.

Original acceptance remains 26/40; 14 production/architecture items remain. UX-R1 is partial: basic navigation/browse is implemented, but dedicated created/completed date controls and native bounded equipment aggregation remain. UX-R2 is partial: dashboard visibility and directory switching are restored, but the inherited portfolio metric links still need a complete filtered-denominator audit. UX-R3 is partial: this is a work-estimate inventory and entry to existing PM/replacement decisions, not a saved budget, future reactive allowance, monthly plan or editable scenario engine. UX-R4 release validation/publication is in progress. Do not call competitive parity or the production checklist complete. Next substantive product work is durable budget/scenario commands and exact portfolio drill-through, reusing current lifecycle and PM records. Keep desktop simplicity and c-store equipment/service language first. Scheduled follow-up stays paused; unrelated dev4.log remains untouched.

Final release validation: corrected typecheck passes, lint passes, Sites and Render builds both exit successfully. Initial full-suite failures are reconciled by the 54-test workflow run and 24-test serial recheck above; total test inventory is now 941 after the additional planning-scope test. Existing duplicate CSS emission warnings remain in the Sites build. Ready to publish the consolidated UX batch; no original production acceptance item is closed.

### C-store UX publication checkpoint

Private Sites version 52 succeeded September 15, 2026 at 20:42:11 UTC, environment revision 1: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Exact pushed application source: f0b754bdc1cd4d0d7ba18c185d9240077a7b42b9. Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_05a41112723c8191bef5312c813f1fa9; deployment appgdep_6aa9ad94b9ac8191ac6bd6c213ccb2d9. Archive validation found 739 entries, the server entry, DB/FILES manifest and 51 migrations; saved archive reports 716 files. Existing private audience retained. No production Sites browser or HTTP probe performed. GitHub codex/platform-rebuild is pushed; this does not establish that Render has deployed the same revision.

UX-R4 is complete for this implemented batch's testing and private publication, not acceptance of the still-partial UX-R1/R2/R3 scope. Original checklist remains 26/40 accepted, 14 open. Next: durable budget/scenario records, exact filtered vendor evidence, work history date controls, and bounded equipment aggregation; preserve the existing planning and lifecycle surfaces. Scheduled follow-up stays paused. dev4.log remains untracked and untouched.

### Small planning/filter follow-up — September 15, 2026

User explicitly asked to keep this follow-up small to conserve usage. Added personal scenarios to the existing planning view using the existing tenant/member-owned saved-view store: target, extra-work allowance and contingency, exact minor-unit arithmetic, scenario total and target difference. Saved values include location/equipment/vendor scope and recalculate against current open repair estimates. Unknown prices remain explicit; PM and replacement totals remain separate. This is a personal comparison, not an approved annual budget, shared financial plan, forecast or frozen cost snapshot. No schema, new dashboard or scenario engine was added.

Work history now supports Created from / Created through (UTC), inclusive through-date, using existing indexed repository predicates. Added the new parameters to the native loader allowlist so they cannot fall back to the old history presenter. Date scope survives Active/History/All navigation. Vendor portfolio measures now open their exact currently filtered comparison/attention sections, with per-vendor visit numerators and denominators; open-work links specify active work. The contact-directory link retains specialty/search/view.

Browser checks caught an existing local saved-view bug: the fixture adapter delegated DELETE to an interpreter that cannot execute it. Replaced that path with an atomic cloned-state update matching SQL's owner/name semantics, and added ownership/replacement regression coverage. Saved-view forms now show pending/errors and refresh after success. Local fictional scenario “Demo repair allowance” saved and reopened with target $50,000, allowance $1,000.10 and contingency $250.20; total $37,900.30 and $12,099.70 below target, with 28 unpriced jobs still visible. Saving the same name leaves one scenario. History Aug 1–25 returns 18 records. HVAC vendor filtering shows two vendors, 179/186 visited jobs and $160,252 recorded cost; source links retain that filtered view.

Validation: seed passed; full run had 942/943 passing with one obsolete vendor-link expectation. Updated the expectation to assert real rendered anchor targets. Workflow checks passed 54/54. Focused follow-ups passed 26 tests, then 21 tests after adding the native-loader regression; fixture scenario and date/money/scope checks pass. Typecheck passed before the final navigation-only polish; both builds include TypeScript verification. Final lint is clean. Final builds/publication follow below. Existing duplicate Sites CSS emission warnings remain. No original production checklist item closed: 26/40 accepted, 14 open. UX-R1 now includes created-date navigation but native equipment aggregation remains deferred; UX-R2's immediate filtered-link gap is fixed; UX-R3 now includes personal saved comparisons, while approved/shared annual budgeting and monthly scenarios remain deliberately outside this small pass. Scheduled continuation stays paused. dev4.log is untouched.

Final follow-up release checks: both Sites and Render builds exit successfully with the final loader/save fixes included. Lint clean; browser viewport reset and the extra agent-created Site tab closed. Publication pending; source is ready.

### Small follow-up publication

Private version 53 succeeded September 15, 2026 at 22:09:21 UTC, environment revision 1: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Application source 18f10158b6e76d81f52c7f95ec88fae56db08c3b pushed to GitHub and Sites. Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_f442f87b0f4c81918ee116deaedb49b1; deployment appgdep_6aa9c2044808819194bfe4104a965f3f. Validated archive: 741 entries, server entry, DB/FILES and 51 migrations; saved version reports 718 files. Private audience preserved. No production Sites probe performed, and this batch's Render deployment is not verified. Final builds and browser evidence above apply. Original acceptance remains 26/40; the lighter scope requested by the user is complete. Scheduled tasks remain paused; unrelated dev4.log untouched.

### Store saved-work and vendor-search repair — September 15, 2026

User clarified that Accountability Only must not offer Save for a later visit. The full edition now exposes that choice directly during work creation, including required service category and review deadline. Store pages in the full edition always show Jobs saved for later with a store-prefilled add link and compatible-job grouping entry. Existing canonical held-work commands and grouping are reused. Accountability routing remains vendor/choose-later only, including when a hold query parameter is supplied.

Vendor search in both editions now displays matching selectable vendors and a result count. Removed the tenant name from searchable coverage labels: previously every vendor matched “pump” because every covered store included Clark Pump and Shop. Gas/gas-pump queries normalize to fuel/dispenser terminology. Existing selected vendors remain visibly marked outside a changed search; selection is never automatic.

Validation: seed, typecheck, lint and both Sites/Render builds passed. Focused edition/presenter checks passed 35/35. Workflow suite initially passed 53/54, with only the old store-section title assertion failing; the corrected assertion passed in the focused run. Full regression run is in progress. Browser: pump returns only PumpPro; created local fictional CPS-2026-2282 as a held Exterior job with future review deadline; it appears in Store 104’s saved-work section, and grouping opens with Store 104 selected and five eligible jobs. No vendor issuance performed. Original acceptance stays 26/40, 14 open. Scheduled tasks stay paused. dev4.log remains untouched. Publication pending.

Final regression result: 945/946 passed; the sole remaining failure expected the old creation heading. Updated that copy assertion; its nine-test file passes. Together with the 35 focused tests above, all observed failures are reconciled. Both builds exited successfully; no application changes after those builds. Ready for publication.

Publication succeeded: private Sites version 54, September 15, 2026 22:40:03 UTC, environment revision 1, https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Pushed source 603f280e4c9fda9b16e44b12f5bc0f858f22681f; version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_0bc4c0fe52588191938faa69bfcb7a9e; deployment appgdep_6aa9c9375a908191bb3d96e93ba9904e. Archive validated with server entry, manifest, 51 migrations, DB/FILES retained. GitHub branch pushed; Render deployment not verified. No production browser probe. Original checklist remains 26/40; scheduled tasks paused.

### Shared button contrast repair — September 15, 2026

Confirmed white-on-white Create work order text after Outside vendor is selected: the primary button’s important white text overrode the secondary save-state color. Scoped save-state rule now sets dark slate text with matching precedence. Also confirmed missing --to-* workspace tokens: they existed only in the retired, unimported traceops.css. Restored the shared token definitions in globals.css without importing legacy page styles; action aliases use the central product brand tokens. This fixes the selected desktop edition button inheriting pale sidebar text on its white background and restores shared workspace color references.

Browser evidence: Create work order now renders rgb(51,65,85) on white; Create and send remains white on cobalt; selected desktop Complete renders rgb(22,31,42) on white. Scanned visible button/button-link foregrounds against ancestor backgrounds on Overview, Work, Stores, Equipment, Vendors and Spend: no contrast ratios below 3 in that sampled normal state. This is not a claim of exhaustive accessibility conformance. Temporary desktop viewport reset. Seed, typecheck and lint passed; required suites and builds running. No new tests added for this CSS-only fix. Checklist remains 26/40 accepted; 14 open. No scheduled tasks resumed; dev4.log untouched.

Full regression suite passed 946/946 across 143 files. Workflow suite and both production builds pending at this checkpoint.

Release validation: 946/946 regression tests, 54/54 workflow tests, seed, typecheck, lint and both Sites/Render builds passed. Application fix f657a3428f068c81dff314a159b114d022f5d69f pushed to GitHub and Sites. Archive validated with server entry, DB/FILES manifest and 51 migrations. Private Sites version 55 saved; publication in progress. Render deployment not verified.

Private publication succeeded September 15, 2026 at 23:02:12 UTC: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_fc50eb2070408191a24822cb7a429ce9; deployment appgdep_6aa9ce651a388191957e31df8e27a5d1; environment revision 1. Existing private audience retained. No production browser probe. Checklist remains 26/40, 14 open; scheduled tasks stay paused.

### Work-order review flow — September 25, 2026

User requested that clicking any normal work-list row cell open the work order, then show the original complaint, submitter, visit history and each visit’s notes at a glance. Normal Active/History/All work-list data cells now share the work-order destination; related store/vendor/cost cell destinations and inline preview buttons no longer intercept that entry. Selection checkboxes remain controls, and the dedicated approved-later management queue keeps its grouping workflow.

The work-order overview now displays chronological visit entries directly below the complaint, with technician/provider, check-in/out, outcome and the recorded note beneath its visit. Visits & notes uses the same grouped presentation; unrelated work-order notes remain separate. Store reports preserve original problem/name/time. Direct-created work shows its creation actor from tenant-scoped audit, with Not recorded when absent, without inventing an original report. Closed cases omit the inactive next-action block and redundant review-this-screen action; Service workflow opens the service record rather than looping to Overview. Per-work-order outcomes/notes on multi-job visits override session-level facts; secondary jobs never borrow the primary job’s note. Recorded note actor/time are preserved when available.

Browser evidence (local fictional tenant): clicked Store 112 in the history row for CPS-2026-2215 and reached that work order; the overview showed creator Jordan Lee and the grounds-service visit note without another click. CPS-2026-0101 showed the original cooler complaint, Submitted by Avery Johnson, original July 5 timestamp and its completed visit note. Desktop layout inspected and temporary viewport reset. No browser mutations. New navigation/linked-job note regression checks passed. Full checks running; an existing held-work test depended on the real calendar despite a fixed September deadline, so its Date clock was pinned to the intended pre-deadline scenario. Production expired-hold behavior is not changed by that test correction. Original acceptance stays 26/40; 14 open. Scheduled tasks remain paused; dev4.log untouched.

Validation checkpoint: full suite 947/948 passed, with the sole calendar-dependent held-work failure corrected by pinning that test’s Date clock. All 25 focused held-work/navigation tests pass afterward. Typecheck and lint pass on the updated source. Workflow suite and both builds are pending; no original production acceptance item is closed.

Final validation: deterministic seed, final typecheck/lint, 54/54 workflow tests and both Sites/Render builds passed. Full-suite 947/948 result is reconciled by the corrected-date 25-test focused run above. Private Sites version 56 succeeded September 25, 2026 at 15:55:06 UTC, environment revision 1: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Application source f16b22894d6af814bc6729022976b3b51cb22dff pushed to GitHub and Sites. Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_7d5f9d6ca93c8191b161e6ef96714c37; deployment appgdep_6ab69949afb48191a9b1bbfbd3487214. Archive checked: server entry, DB/FILES and 51 migrations. Existing private audience preserved; no production browser probe. Render deployment not verified. Checklist remains 26/40; 14 open.

### Workspace loading repair — September 25, 2026

User reported a workspace loading error. The local overview loaded normally; the open Sites overview reproduced the workspace error after retry. Hosted D1 was reachable. Dashboard lifecycle batches contained 100 asset IDs plus tenant and active-profile parameters, exceeding D1's documented 100-parameter limit. Reproduced the failure by enforcing that limit in the existing native-SQL lifecycle regression, then reduced the asset batch to 98 with consistent pagination. The regression passes, retains full fixture parity, finds the candidate beyond three pages, and checks tenant isolation. No data reset or migration. Required checks and hosted recovery verification pending. Checklist remains 26/40 accepted, 14 open; scheduled tasks stay paused; dev4.log untouched.

The first repair passed 948/948 tests, 54/54 workflows and both builds but did not restore the hosted overview. Added server-side failure logging; the stack identified the attention query. Miniflare reproduced D1's five-term compound SELECT limit (the queue had six populations). Split the union into two CTEs while preserving filters, totals and ordering. A new real-D1 integration regression now seeds the full migrated fixture and confirms review-queue/lifecycle parity for company, store and foreign-tenant scopes; passed in 45 seconds. Final checks and hosted verification pending. No original acceptance item closed.

Final repair source 7d11a4a8164ac2582d8ed7040189139154fd591d is pushed to GitHub and Sites. Full regression 949/949 (144 files), earlier workflow run 54/54, seed, typecheck, lint (one diagnostics JSX lint issue corrected and file rechecked), Sites build and Render build passed. Actual D1 integration also passed independently. Two archive-upload timeouts were retried without rebuilding; private publication succeeded September 25, 2026 at 16:41:42 UTC, environment revision 1. Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_912f9a21523881918b57fd335a1f0e0a; deployment appgdep_6ab6a43873b08191adfd58f22dd9f1f0. Hosted overview now renders Maintenance overview with 15 stores, 62 review items and 399 visits; direct review queue also loads its 62 persisted records. Existing hosted data retained (therefore totals differ from the resettable local fixture). DB/FILES and private audience preserved. Render deployment not verified. Original acceptance remains 26/40, 14 open; scheduled tasks paused; dev4.log untouched.

Hosted follow-up found a separate production link failure: Vinext dynamically imported navigation helpers from a client entry whose exports had been dropped. Browser console reported undefined prefetch/navigation functions, and link clicks left the URL unchanged even though direct routes loaded. Set Vite/Rolldown preserveEntrySignatures to strict. Production output now retains the navigation namespace and maps dynamic imports to it; config lint and rebuild passed. Live click verification pending.

Navigation release f9d635776a4a1fc421bef46835289e33426a91f9 pushed to GitHub/Sites and published privately at 16:47:10 UTC. Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_7eca0016f4208191a34294ad4f4b79fb; deployment appgdep_6ab6a5816dbc819183a0c825a432de3f; environment revision 1. Reloaded the hosted review queue and clicked Work orders: the browser navigated and rendered the actual work-order list, confirming the previously failed client-link path now works. This supersedes the pending click check above. No additional domain changes after the 949-test run; the build-setting correction was verified by production build output, config lint and live navigation. Workspace-loading repair complete; checklist totals remain unchanged.

### Compact overview spending visual — September 25, 2026

User requested a more visual overview and removal of the large review queue while retaining its tile. Maintenance and regional overviews now show Where maintenance dollars go: recorded cost by service area beside the highest-cost stores. Uses existing scoped/paginated aggregates and exact source links; no new data fetch or analytics subsystem. The review tile remains; its duplicate header button is replaced by Create work order. Category/store visual appears once per overview; existing monthly trend supplies the time view. Labels, amounts, shares, full-cohort coverage and visible reporting period are preserved; empty-cost state is explicit. Personal store workflows and existing owner/finance layouts remain intact.

Local visual inspected at the actual in-app viewport (the requested desktop viewport override did not change that tab's dimensions); category bar click opened work orders with category, hasCost, dates and USD preserved, with no browser errors. Fourteen focused dashboard source/render checks pass; seed, typecheck and lint pass. Corrected a pre-existing type inference error in the prior Vite preserveEntrySignatures setting with a literal assertion; runtime behavior unchanged. Broader checks and publication pending. Original checklist remains 26/40; 14 open; no scheduled tasks resumed; dev4.log untouched.

Final validation: 949/949 regression checks, 54/54 workflow checks, 14 focused dashboard checks, seed, typecheck, lint, and Sites/Render builds passed. Final Sites rebuild includes the header-action simplification. Source 02fa1fcbdcb4bd44b8b02d0b8c5750bf0abf2703 pushed to GitHub and Sites. Private publication succeeded September 25, 2026 16:57:20 UTC, environment revision 1: https://clarks-operations-demo.kedrick-mckenzie.chatgpt.site . Version appgprj_6a733d7dbb708191a9b80f3f9424582a~appgver_202bc91ad0188191ad7d2155260b2d69; deployment appgdep_6ab6a7e450e481919d31a70494eb50ab. DB/FILES, 51 migrations and private audience retained; Render deployment not verified. Checklist remains 26/40 accepted, 14 open.

### Render store-save origin repair — September 25, 2026

User confirmed the rejected Add store form is on Render. Preserved their existing filled form without reloading or submitting it. The shared mutation guard compared public Origin directly to the server-facing request URL. A real store-route regression reproduced HTTP 403 when an authenticated, companywide browser submission used a public HTTPS origin behind an internal HTTP URL. Added shared Fetch Metadata origin validation: browser-owned Sec-Fetch-Site same-origin is accepted behind proxies; cross-site, opaque/malformed Origin and mismatched Origin without that evidence remain rejected. Forwarded-host headers do not grant trust. Organization selection reuses the same helper. Membership, scope, role and read-only checks remain in force. Focused security/store-route checks passed 41/41, including actual store persistence and relative 303 redirect in an isolated fixture. Final checks pending.

Render dashboard browser session was signed out; attempted existing GitHub sign-in reached a blank login. User asked to sign in so deployment can proceed. No production store created and entered fields remain intact. This request targets Render; no Sites publication is requested. Original checklist remains 26/40 accepted, 14 open; scheduled tasks stay paused and dev4.log untouched.

Render validation and deployment complete: seed, typecheck, lint, Render build and Sites build passed; 959/959 regression tests (145 files) and 54/54 workflow checks passed. User signed into Render. Source 8630c045771333ca44354ba65950a7a2553a5821 pushed to GitHub and explicitly deployed to traceops-convenience-demo on codex/platform-rebuild. Deployment dep-daranbtg1s2s73annp2g reported service live September 25, 2026 at 13:16:27 Eastern. Live browser verification loaded Maintenance overview, the compact spending visual, review tile and 15-store scope. This also brings the previously Sites-only overview/navigation/work-order improvements to Render. Store-save behavior verified in the isolated proxy regression; the user's filled live form was deliberately not resubmitted or reloaded, so their live save remains to retry. No live store created. No Sites publication, settings/auth changes or automation changes. Checklist remains 26/40 accepted, 14 open; dev4.log untouched.

### Multi-store PM schedules — September 25, 2026

User approved one PM schedule across selected stores with automatic service-area coverage and store exceptions. New schedules use one existing PM plan per store and a typed coverage rule in the existing asset_selection_rule field; no parallel PM stack or migration. Category membership includes future equipment, excludes retired records, and accepts explicit local additions/exclusions. Store service remains possible with an incomplete equipment list. Work-order creation snapshots covered equipment into PM work items and authorization scope; one canonical work order per occurrence remains enforced. Existing equipment-specific schedules and history are retained.

Creation now offers category, frequency, first date, optional instructions, compact region-filtered multi-store selection and coverage counts. Store customization supports equipment, interval/window, preferred vendor, instructions, removal and reset to schedule defaults. Company edits create versioned program/checklist records, reuse existing store plans, preserve only explicit timing overrides (equipment/instruction exceptions do not freeze timing), and create occurrences only for newly added stores. Recurrence uses the plan's current program version. Store pages link directly to their PM plans; PM occurrence pages show covered equipment; company schedules expose Edit schedule. General PM creation enters the new multi-store form for companywide managers, while asset-specific and scoped store-role entry points retain their existing single-plan flow.

Verified locally in the browser: selected HVAC and two stores (three units), created two store plans, saved/reopened Store 101 equipment exclusion, ColdLine vendor preference and roof-access instructions, and edited the company interval without duplicating store plans. Compact desktop form visually inspected. 964/964 regression tests and 54/54 workflow checks passed. Additional real SQLite persistence regression caught and corrected checklist-version uniqueness on company edits; six focused domain/persistence tests then passed, plus SQL coverage query parity. Final builds and publication pending. Original checklist stays 26/40 accepted, 14 open; no automation started; dev4.log untouched.

Final PM release: source 6bc0945c07f1c89c5cd135523681e0da9a42470c committed and pushed to GitHub. Seed/typecheck/lint and both Render/Sites builds passed; full run 964/964 and workflow run 54/54 passed, followed by seven focused coverage/persistence/query checks after final refinements (including the additional SQL persistence test). Render deployment dep-darb8c893c1s73etl90g reported live September 25, 2026 at 13:53:03 Eastern. Published form verified in Chrome: Create PM schedule, service area, region-filtered store list, equipment totals, and Create schedule control; desktop screenshot inspected. Live scope now contains 16 stores from existing user data; no live stores, equipment or schedules were created by this task, and the deterministic presentation seed remains 15 stores/five vendors. The live create form was left open without submission. Sites was not republished; this release targets Render. Original 26/40 checklist and 14 open items remain unchanged.


### Live search and store-specific equipment — September 25, 2026

Added live PM store search with selection preservation, searchable request/work-order store pickers, debounced server-backed list searches (including review, PM, vendor performance and request linking), and a scoped workspace search dropdown. Existing role/tenant search rules and list filters are reused. Work-order creation now restricts equipment to the selected store/category, excludes retired equipment and clears the old equipment selection when the store changes. Request/visit/PM-bound stores remain fixed.

Local browser verified: PM Cedar search narrows to Store 101 while preserving 15 selected stores; work-order store search narrows instantly; Store 101 equipment choices are limited to 101, changing to 102 clears the selection and shows only 102 equipment; workspace ColdLine search displays linked live results; work-order list search narrows to CPS-2026-0116 without submission and retains open-status scope and input focus. No live records created.

Vendor-question command inspection and regression confirm the question records a response and a follow-up task on the original work order without changing the set of work-order IDs. Receipt now names that work-order number. Actual reported duplicate is not yet identified: user was asked for the two numbers. Do not claim that specific incident is resolved without identifying it. Required final validation and Render deployment pending. Original checklist remains 26/40 accepted, 14 open; no automation resumed; dev4.log preserved.

Validation: seed, typecheck, lint and both Render/Sites builds passed. Full run: 961/965 passed initially; four failures were static-render tests missing the newly required Next router mock. Added router mocks in the three affected test files; all 22 tests across those files pass on focused reruns. All 54 workflow checks passed. No domain or production behavior changed to satisfy the rendering harness. Desktop PM and live search dropdown screenshots inspected. Render publication pending.

Release complete: application commit 661952b9f008e2e17747844fe37809065a02dcc4 pushed and deployed to Render. Deployment dep-darbnvh7lnhs73cot520 reported Live September 25, 2026 at 14:26:37 Eastern. Published work-order form contains Find a store; typing ColdLine in the workspace header returned live work-order/vendor results from the Render backend. No live records created, no Sites publication, no settings/auth changes. Vendor incident remains awaiting the specific record numbers; original checklist totals unchanged.


### Vendor correspondence and spending visibility — September 25, 2026

User approved questions after service starts or finishes without reopening work, and requested stronger Spending & planning. Vendor questions now append correspondence and a non-blocking Facilities reply task without replacing service tasks, status or accountability. Replies complete only the corresponding question task; completed work remains completed. Public gateway, domain guards, operator reply loader and UI updated together. Valid current service links can read completed work for questions; expired/revoked/superseded/cancelled links remain rejected. Completed-work links cannot enter the technician visit channel. Existing final-closure token revocation remains; revoked links are not resurrected.

Live Render inspection confirmed spending data and monthly visual still exist ($114,895 recorded work cost in the selected six-month scope); findings were below the main content and planning was mostly a table. Reused existing scoped models: overview now leads with totals/findings, retains monthly comparison, adds source-linked spending-driver bars, and puts lengthy insight explanations behind Details. Planning shows priced/total job coverage, missing prices and explicit estimate exclusions; historical executive results no longer appear as current planning totals. No synthetic financial totals or annualized forecast added. Local driver click verified Store 104 records with period/currency/cost basis preserved; planning coverage checked in browser. Original checklist remains 26/40 accepted,14 open. No automation, no live records created; dev4.log preserved. Final checks and Render release pending.

Validation: 970/971 initially passed; the sole failure expected completed service links to disappear under the old policy. Updated that public boundary regression to require question access with unchanged completed status and prohibited check-in; all 12 boundary tests pass, and an additional acceptance-then-question test passes. Six new domain cases cover accepted, onsite, parts, review, resolved and closed work, including replies and preservation of existing tasks. All 54 workflow tests passed. Seed, typecheck, lint passed. Browser accepted a local demo authorization, returned to the question-only form, submitted a question and received the same CPS-2026-0116 reference. Fixed same-page receipt return with a normal document navigation. Desktop spending and planning screenshots inspected. Final builds and publication pending.

Released application commit 069388ca0fcc62809579f13d0476941180ebfba3 to Render. Final Render/Sites builds and lint passed (final estimate-label wording clarified afterward; Render rebuilt that exact release). Deployment dep-darc63vavr4c73e2vos0 reported Live September 25, 2026 at 14:56:12 Eastern. Live Spending overview verified with unchanged $114,895 scoped recorded cost, leading findings, collapsed Details and Where the money goes visual. No live data mutations and no Sites publication. Checklist totals unchanged.


### Warranty entry, spending comparison and internal cost flags — September 25, 2026

User requested clear comparison wording/placement, multiple manufacturer/vendor equipment/component warranties, and a separate internal work-order flag. Comparison amount and exact dates now sit with the current result, using the selected comparison mode; the detached comparison tile is removed. Calculations/source links remain unchanged.

Equipment now has an Add warranty flow for manufacturer or vendor-work coverage, whole equipment or a selected component, named provider, individual dates, separate parts/labor terms, optional work-order link and claim instructions. Entries append with transactional audit/outbox and scoped actor/asset/component/work validation. Existing warranty storage is extended compatibly with provider kind (existing rows default manufacturer), title and optional links; no prior records are rewritten. The equipment coverage table precedes service history, with dates/status, collapsed terms, provider names and add controls. Component detail links into the same flow. The equipment summary links to individual coverage records instead of implying one expiry describes all warranties. Warranty-case review and future vendor rules remain distinct; recording coverage does not itself establish claim eligibility or fabricate a verified repair.

New work orders support an independent internal recorded-cost flag under Add service details, including normal routing and accountability-only forms. It is persisted separately from NTE and not added to vendor authorization. Cumulative same-currency recorded costs above the amount create one open, non-blocking Facilities review task atomically with the cost; exactly at the threshold does not flag. Existing held-work invoice threshold retains its separate meaning. This change does not treat invoice totals as recorded work costs or approve/block vendor charges.

Validation: seed/typecheck/lint passed. Full suite: 976 passed, one outdated D1 migration-count assertion corrected for migration 52; both D1-chain tests then passed. Five new focused regressions passed (multiple warranties, invalid component/date/tenant, regional scope, cumulative internal flag and currency separation). All 56 workflow/public-boundary/presenter/PostgreSQL checks passed, including a new PostgreSQL persistence and threshold-trigger check. Final Render and Sites builds passed, including the display polish. Browser saved manufacturer component coverage plus separate vendor-work coverage on one local equipment record and verified both remain visible; creation form shows internal flag separate from NTE. No live records changed. Original checklist remains 26/40 accepted, 14 open. No scheduled tasks or subagents. Render deployment pending; dev4.log preserved.
